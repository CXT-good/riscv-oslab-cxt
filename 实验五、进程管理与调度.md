# 实验五、进程管理与调度

## 任务1：深入理解进程抽象

### **1. 进程结构体各字段的作用**

| 字段名      | 类型                 | 作用                                                         |
| :---------- | :------------------- | :----------------------------------------------------------- |
| `lock`      | `struct spinlock`    | 保护进程结构体自身的并发访问，防止数据竞争。                 |
| `state`     | `enum procstate`     | 标识进程的当前状态，是调度和管理的核心依据。                 |
| `chan`      | `void *`             | 记录进程因等待何种“事件”而进入睡眠状态（如等待磁盘I/O完成、等待子进程退出）。 |
| `killed`    | `int`                | 一个标记位，表示该进程是否被“杀死”（如收到kill信号）。并非立即终止，而是在下次进入内核时检查并处理。 |
| `xstate`    | `int`                | 进程的退出状态码，由其自身或父进程设置，供 `wait()` 系统调用读取。 |
| `pid`       | `int`                | 进程的唯一标识符（Process ID）。                             |
| `pagetable` | `pagetable_t`        | 指向该进程的**用户空间页表**。进程切换时，需要切换页表以隔离地址空间。 |
| `trapframe` | `struct trapframe *` | 指向一个保存了**用户寄存器上下文**的内存页。当从用户态陷入内核时，所有用户寄存器被保存于此；返回用户态前，从这里恢复。 |
| `context`   | `struct context`     | 保存了**内核调度上下文**（如callee-saved寄存器、栈指针）。在**内核态**下进行进程切换时，用于保存和恢复内核的执行状态。 |

**关键区别**：

**`trapframe`**：保存的是**用户态**的执行现场（系统调用、中断时使用）。

**`context`**：保存的是**内核态**的执行现场（调度器切换进程时使用）。

---

### **2. 进程状态转换图**

```
UNUSED <--> USED <--> RUNNABLE <--> RUNNING --> SLEEPING --> ZOMBIE
                (allocproc)     (scheduler)   (sleep)     (exit)
```

| 状态转换                | 触发条件                                                     |
| :---------------------- | :----------------------------------------------------------- |
| **UNUSED → USED**       | `allocproc()` 成功从进程表中找到一个空闲槽位并初始化。       |
| **USED → RUNNABLE**     | `fork()` 完成子进程的创建，或 `sleep()` 的进程被 `wakeup()`。 |
| **RUNNABLE → RUNNING**  | 调度器 `scheduler()` 选中该进程，并执行上下文切换。          |
| **RUNNING → RUNNABLE**  | 时间片用完（时钟中断），或进程主动调用 `yield()`。           |
| **RUNNING → SLEEPING**  | 进程因等待事件（如I/O、子进程退出）而调用 `sleep()`。        |
| **SLEEPING → RUNNABLE** | 等待的事件发生，其他进程或中断处理程序调用 `wakeup()`。      |
| **RUNNING → ZOMBIE**    | 进程调用 `exit()` 终止自己。                                 |

---

### **3. 为什么需要锁保护？**

**并发访问**：在多核/多线程环境中，多个CPU可能同时操作进程表或同一个进程结构体（例如，一个CPU正在执行 `exit()`，而另一个CPU正在读取该进程的状态）。

**数据一致性**：锁确保了进程状态（`state`）、引用计数、链表指针等关键数据的修改是原子的，防止出现不一致的状态。

**防止竞态条件**：例如，在调度器将进程状态从 `RUNNABLE` 改为 `RUNNING` 的过程中，如果没有锁保护，另一个CPU可能同时将其状态改为 `SLEEPING`，导致调度错误。

---

### **4. 每个状态转换的触发条件**

![image-20251103155227322](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251103155227322.png)

---

### **5. 哪些操作需要原子性保护？**

所有会修改进程结构体中共享数据的操作都需要在持有锁的情况下进行，例如：

（1）修改 `state`、`killed`、`chan`、`xstate` 等字段。

（2）从进程表中分配或释放一个进程槽（`allocproc()` / `wait()`）。

（3）修改进程的父进程指针、子进程链表等关系数据。

（4）对进程的页表、打开文件表等资源的引用计数进行操作。

---

### **深入思考**

#### **1. 为什么需要ZOMBIE状态？**

它的核心目的是**保留进程的退出状态码（`xstate`）**，直到其父进程通过 `wait()` 系统调用来查询。

**如果没有ZOMBIE状态**：进程在 `exit()` 时立即释放所有资源（包括 `struct proc` 本身）。那么，如果父进程稍后调用 `wait()`，它将无法获取已不存在的子进程的退出状态。

**ZOMBIE状态的作用**：进程在 `exit()` 后，释放了大部分资源（内存、文件等），但保留了 `struct proc` 槽位和 `xstate`，父进程 `wait()` 后，才最终释放 `struct proc` 槽位，状态变为 `UNUSED`。

#### **2. 进程表的大小限制有什么影响？**

`NPROC`（进程表大小）决定了系统**同时存在的最大进程数**。

**影响**：

（1）**资源限制**：限制了系统的并发能力。一旦进程表满，`fork()` 等创建新进程的操作将失败。

（2）**设计简化**：使用静态数组使得实现简单，但缺乏动态扩展性。

（3）**拒绝服务**：恶意或 buggy 的程序可能通过不断创建进程（但不退出）来耗尽进程表，导致系统无法创建新进程。

#### **3. 如何防止进程ID重复？**

xv6通常采用简单的**单调递增**策略来分配PID：

（1）维护一个全局变量 `next_pid`。

（2）在 `allocproc()` 中，为新进程分配 `next_pid`，然后执行 `next_pid++`。

（3）当 `next_pid` 达到最大值（如 `MAX_PID`）时，回绕到最小值重新开始。

**如何防止重复？**

​	在回绕时，需要确保之前分配的、与新的PID相同的那个进程已经处于 `UNUSED` 状态（即其 `struct proc` 槽位已被回收）。在实践中，由于PID空间通常远大于进程表大小（`NPROC`），在PID回绕之前，之前占用该PID的进程早已终止并被回收，因此重复的概率极低。这是一种简单而有效的策略。

基于提供的代码，我们来详细分析xv6的进程创建机制。





## 任务2：分析xv6的进程创建机制

### **1. allocproc() 函数分析**

**（1）如何在进程表中找到空闲槽位？**

```c
for(p = proc; p < &proc[NPROC]; p++) {
  acquire(&p->lock);
  if(p->state == UNUSED) {
    goto found;
  } else {
    release(&p->lock);
  }
}
```
**方法**：线性扫描整个进程表 `proc[NPROC]`，检查每个进程的 `state` 字段。

**找到后**：立即获取该进程的锁，跳转到 `found` 标签进行初始化。

**未找到**：释放当前进程的锁，继续查找，最终返回0表示失败。

**（2）进程ID是如何分配的？**

```c
p->pid = allocpid();

int allocpid() {
  int pid;
  acquire(&pid_lock);
  pid = nextpid;
  nextpid = nextpid + 1;
  release(&pid_lock);
  return pid;
}
```
**机制**：使用全局变量 `nextpid`，通过锁 `pid_lock` 保护，保证分配的原子性。

**特点**：PID单调递增，从1开始。

**（3）用户栈是如何设置的？**

在 `allocproc()` 中**没有直接设置用户栈**。用户栈的设置在：

**首次创建进程时**（如 `userinit()`）：通过其他机制设置初始用户栈。

**fork时**：在 `kfork()` 中通过 `uvmcopy()` 复制父进程的整个用户空间，包括用户栈。

**（4）陷阱帧的初始化过程**

```c
// 分配陷阱帧页面
if((p->trapframe = (struct trapframe *)kalloc()) == 0){
  freeproc(p);
  release(&p->lock);
  return 0;
}

// 在proc_pagetable()中映射陷阱帧
if(mappages(pagetable, TRAPFRAME, PGSIZE,
            (uint64)(p->trapframe), PTE_R | PTE_W) < 0){
  // 错误处理...
}
```
**步骤**：

（1）分配物理页面给陷阱帧

（2）在进程页表的固定地址 `TRAPFRAME` 处映射该页面

（3）后续在fork时复制父进程的陷阱帧内容



### **2. fork() 实现分析**

**fork的完整流程**

```c
int kfork(void) {
  // 1. 分配新进程结构
  if((np = allocproc()) == 0) return -1;
  
  // 2. 复制用户内存
  if(uvmcopy(p->pagetable, np->pagetable, p->sz) < 0){
    freeproc(np); release(&np->lock); return -1;
  }
  
  // 3. 复制陷阱帧
  *(np->trapframe) = *(p->trapframe);
  
  // 4. 设置返回值（子进程返回0）
  np->trapframe->a0 = 0;
  
  // 5. 复制文件描述符等资源
  for(i = 0; i < NOFILE; i++) ...
  
  // 6. 设置父子关系
  np->parent = p;
  
  // 7. 标记为RUNNABLE
  np->state = RUNNABLE;
  
  return np->pid;
}
```

**（1）为什么父子进程有不同的返回值？**

```c
// 子进程返回0
np->trapframe->a0 = 0;

// 父进程返回子进程PID
return np->pid;
```
 **原理**：RISC-V调用约定中，返回值存放在a0寄存器。

子进程的陷阱帧中a0被设为0，当它第一次被调度时，从陷阱帧恢复寄存器，自然返回0。

父进程直接返回子进程的PID。

**（2）内存复制是如何实现的？**

通过 `uvmcopy(p->pagetable, np->pagetable, p->sz)`：

**机制**：遍历父进程的页表，为每个有效的用户页面：

（1）分配新的物理页面

（2）复制页面内容

（3）在子进程页表中建立相同映射

**特点**：这是**物理内存的完全复制**，性能开销大。

**（3）失败时的资源清理策略**

```c
if(uvmcopy(p->pagetable, np->pagetable, p->sz) < 0){
  freeproc(np);
  release(&np->lock);
  return -1;
}
```
- **策略**：一旦某个步骤失败（如内存不足），立即：
  
  （1）调用 `freeproc(np)` 释放已分配的资源
  
  （2）释放进程锁
  
  （3）返回错误码
  
  

### **3. 进程退出机制分析**

**（1）exit() 与 wait() 的协作关系**

```c
// exit() 关键部分
p->xstate = status;      // 保存退出状态
p->state = ZOMBIE;       // 变为僵尸状态
wakeup(p->parent);       // 唤醒等待的父进程
sched();                 // 永久放弃CPU

// wait() 关键部分  
for(pp = proc; pp < &proc[NPROC]; pp++){
  if(pp->parent == p && pp->state == ZOMBIE){
    // 找到僵尸子进程
    pid = pp->pid;
    // 复制退出状态给用户
    copyout(p->pagetable, addr, (char *)&pp->xstate, sizeof(pp->xstate));
    freeproc(pp);        // 最终释放子进程资源
    return pid;
  }
}
// 没有僵尸子进程则睡眠
sleep(p, &wait_lock);
```
**协作机制**：

子进程 `exit()`：设置状态码 → 变僵尸 → 唤醒父进程 → 永久睡眠

父进程 `wait()`：查找僵尸子进程 → 读取状态码 → 清理资源

如果没有僵尸子进程，父进程在 `sleep()` 中等待

**（2）资源回收的时机和方式**

**时机**：在父进程调用 `wait()` 时最终完成回收

**方式**： 

```c
void freeproc(struct proc *p) {
  if(p->trapframe) kfree((void*)p->trapframe);
  if(p->pagetable) proc_freepagetable(p->pagetable, p->sz);
  // 重置所有字段为0
  p->state = UNUSED;
}
```

**（3）孤儿进程的处理**

```c
void reparent(struct proc *p) {
  for(pp = proc; pp < &proc[NPROC]; pp++){
    if(pp->parent == p){
      pp->parent = initproc;  // 过继给init进程
      wakeup(initproc);       // 唤醒init来回收
    }
  }
}
```
**机制**：在进程退出时，如果它有未退出的子进程，这些子进程会被**过继给init进程**（PID=1）

**目的**：确保所有进程最终都能被正确回收，避免僵尸进程永远存在



### **关键问题解答**

**（1）fork()的性能瓶颈在哪里？**

1. **内存复制开销**：`uvmcopy()` 需要复制整个用户地址空间，对于大进程开销巨大
2. **页表操作**：需要为子进程构建完整的页表结构
3. **物理页面分配**：需要分配大量物理页面并复制内容

**（2）如何实现写时复制优化？**

**写时复制（Copy-on-Write, CoW）优化方案**：

1. **页表项权限调整**：
   
   在fork时，不复制物理页面，而是让父子进程共享同一物理页面

   将共享页面的PTE权限设为**只读**
   
2. **写保护异常处理**：
   
   当任一进程尝试写入只读页面时，触发页错误
   
   在页错误处理程序中：
   
   ​	分配新的物理页面

   ​	复制原页面内容
   
   ​	更新故障进程的PTE指向新页面，并设为可写
   
   ​	继续执行被中断的写入指令
   
3. **引用计数管理**：
   
   为每个物理页面维护引用计数
   
   当最后一个引用释放时才真正释放物理页面

这种优化可以**显著减少fork的开销**，特别是对于创建后立即exec的常见用例。



## 任务3：设计你的进程管理系统

### **1. 进程结构体设计**

```c
// 进程状态定义
enum procstate { 
    UNUSED,     // 未使用
    EMBRYO,     // 创建中（新增中间状态）
    RUNNABLE,   // 可运行
    RUNNING,    // 运行中
    SLEEPING,   // 睡眠中
    ZOMBIE      // 僵尸状态
};

// 进程结构体
struct proc {
    // 同步保护
    struct spinlock lock;
    
    // 标识信息
    int pid;                    // 进程ID
    int ppid;                   // 父进程ID
    char name[16];              // 进程名
    
    // 状态管理
    enum procstate state;       // 进程状态
    void *chan;                 // 睡眠通道
    int killed;                 // 终止标记
    int xstate;                 // 退出状态码
    
    // 内存管理
    pagetable_t pagetable;      // 页表指针
    uint64 sz;                  // 进程内存大小
    uint64 stack_base;          // 用户栈基址（新增）
    
    // 执行上下文
    struct trapframe *trapframe; // 用户态上下文
    struct context context;     // 内核态上下文
    uint64 kstack;              // 内核栈
    
    // 资源管理
    struct file *ofile[NOFILE]; // 打开文件表
    struct inode *cwd;          // 当前工作目录
    
    // 进程关系（扩展）
    struct proc *parent;        // 父进程指针
    struct proc *children;      // 子进程链表头
    struct proc *sibling;       // 兄弟进程链表
    
    // 资源限制（扩展）
    struct rlimit rlim[RLIM_NLIMITS]; // 资源限制数组
    
    // 统计信息（扩展）
    uint64 utime;               // 用户态运行时间
    uint64 ktime;               // 内核态运行时间
    uint64 start_time;          // 创建时间戳
};
```

---

### **2. 进程表组织方式**

**选择：数组 + 空闲链表**

```c
#define NPROC 64

// 全局进程表
struct proc proc[NPROC];

// 空闲进程链表（优化分配效率）
struct proc *freelist;

// 进程哈希表（优化PID查找）
struct proc *pid_hash[PID_HASH_SIZE];
```

**理由**：

**数组**：实现简单，内存连续，访问快速

**空闲链表**：避免分配时的线性扫描，提高 `alloc_process()` 效率

**PID哈希表**：支持O(1)时间复杂度的PID查找



### **3. 进程ID分配策略**

```c
// PID分配管理结构
struct pid_allocator {
    struct spinlock lock;
    int next_pid;               // 下一个分配的PID
    int pid_recycle;            // 是否启用PID回收
    bitmap_t pid_map;           // PID位图（扩展支持）
};

// 分配策略：单调递增 + 有限回收
int alloc_pid(void) {
    acquire(&pid_lock);
    
    // 尝试从next_pid开始分配
    for (int i = 0; i < MAX_PID; i++) {
        int pid = next_pid;
        next_pid = (next_pid + 1) % MAX_PID;
        
        if (is_pid_available(pid)) {
            mark_pid_used(pid);
            release(&pid_lock);
            return pid;
        }
    }
    
    release(&pid_lock);
    return -1;  // PID耗尽
}
```

**特性**：

**范围**：1 ~ MAX_PID-1（0保留给内核）

**回收**：进程退出时立即回收PID（相比xv6的延迟回收）

**冲突避免**：使用位图确保不重复分配

---

### **4. 核心接口设计**

```c
// ==================== 进程生命周期管理 ====================
struct proc* alloc_process(void);
void free_process(struct proc *p);
int create_process(void (*entry)(void), char *name, int flags);
void exit_process(int status);
int wait_process(int *status);

// ==================== 进程关系管理 ====================
int fork_process(void);                            // 复制当前进程
int exec_process(char *path, char **argv);         // 执行新程序
int kill_process(int pid, int signal);             // 发送信号
struct proc* find_process(int pid);                // 根据PID查找进程

// ==================== 进程树管理 ====================
void reparent_children(struct proc *p);            // 重新设置子进程父关系
void add_child_process(struct proc *parent, struct proc *child);
void remove_child_process(struct proc *parent, struct proc *child);

// ==================== 资源限制管理 ====================
int set_rlimit(int resource, struct rlimit *new_limit);
int get_rlimit(int resource, struct rlimit *old_limit);

// ==================== 进程信息查询 ====================
int get_process_info(int pid, struct proc_info *info);
void list_processes(struct proc_list *list);
```

---

### **5. 设计问题解决方案**

**Q1: 进程表用数组还是链表？**

**答案**：混合方案

**主存储**：静态数组，保证内存连续性和缓存友好

**空闲管理**：链表连接空闲槽位，提高分配效率

**关系管理**：链表维护父子、兄弟关系

**Q2: 如何高效查找特定PID的进程？**

**方案**：PID哈希表

```c
#define PID_HASH(pid) ((pid) % PID_HASH_SIZE)

struct proc* find_process(int pid) {
    int bucket = PID_HASH(pid);
    struct proc *p = pid_hash[bucket];
    
    while (p != NULL) {
        if (p->pid == pid && p->state != UNUSED) {
            return p;
        }
        p = p->hash_next;
    }
    return NULL;
}
```

**Q3: 是否需要进程组和会话的概念？**

**简化方案**：基础支持

```c
// 基础进程组支持
int pgid;                   // 进程组ID
struct proc *group_leader;  // 进程组首领

// 会话支持（可选）
int sid;                    // 会话ID
```
**理由**：为后续实现shell作业控制、信号广播等功能奠定基础

**Q4: 如何处理进程资源限制？**

**方案**：资源限制结构体

```c
struct rlimit {
    uint64 rlim_cur;    // 当前软限制
    uint64 rlim_max;    // 最大硬限制
};

// 支持的限制类型
enum {
    RLIMIT_CPU,     // CPU时间（秒）
    RLIMIT_FSIZE,   // 文件大小
    RLIMIT_DATA,    // 数据段大小
    RLIMIT_STACK,   // 栈大小
    RLIMIT_NOFILE,  // 打开文件数
    RLIMIT_NPROC,   // 子进程数
    RLIM_NLIMITS
};
```

---

### **6. 实现策略**

**阶段1：基础进程创建和销毁**

1. 实现 `alloc_process()` / `free_process()`
2. 实现静态进程表管理
3. 实现基本的PID分配
4. 测试单个进程的创建和退出

**阶段2：父子关系管理**

1. 实现 `fork_process()` 和进程复制
2. 建立进程树关系（父子、兄弟链表）
3. 实现 `wait_process()` 和僵尸进程回收
4. 实现孤儿进程过继机制

**阶段3：性能优化和扩展功能**

1. 添加空闲进程链表优化分配
2. 实现PID哈希表加速查找
3. 添加资源限制框架
4. 实现进程统计信息收集

**阶段4：高级特性（可选）**

1. 进程组和会话管理
2. 信号处理框架
3. 写时复制优化
4. 进程能力控制





## 任务4：实现上下文切换机制

### **1. 上下文切换的本质**

**哪些寄存器需要保存？**

从代码中可以看到保存的寄存器：
```asm
sd ra, 0(a0)    # 返回地址
sd sp, 8(a0)    # 栈指针
sd s0, 16(a0)   # 被调用者保存寄存器
sd s1, 24(a0)
sd s2, 32(a0)
sd s3, 40(a0)
sd s4, 48(a0)
sd s5, 56(a0)
sd s6, 64(a0)
sd s7, 72(a0)
sd s8, 80(a0)
sd s9, 88(a0)
sd s10, 96(a0)
sd s11, 104(a0)
```

**保存的寄存器类型**：

**ra**：返回地址寄存器

**sp**：栈指针寄存器  

**s0-s11**：12个被调用者保存寄存器

**为什么不保存所有寄存器？**

**RISC-V寄存器分类**：

| 类型             | 寄存器         | 是否需要保存 | 原因                 |
| ---------------- | -------------- | ------------ | -------------------- |
| **调用者保存**   | a0-a7, t0-t6   | ❌ 不需要     | 由调用函数负责保存   |
| **被调用者保存** | s0-s11, ra, sp | ✅ 需要       | 由被调用函数保证不变 |
| **临时寄存器**   | zero, gp, tp   | ❌ 不需要     | 特殊用途或不需要保存 |

**设计原理**：

**调用者保存寄存器**：在函数调用前后值可能改变，调用者根据需要自行保存

**被调用者保存寄存器**：函数必须保证这些寄存器在调用前后保持不变

**调用者保存 vs 被调用者保存的区别**

| 方面           | 调用者保存                          | 被调用者保存                 |
| -------------- | ----------------------------------- | ---------------------------- |
| **责任方**     | 调用函数负责保存                    | 被调用函数负责保存           |
| **保存时机**   | 在调用其他函数之前                  | 在函数开始时保存，结束时恢复 |
| **典型寄存器** | a0-a7（参数/返回值）, t0-t6（临时） | s0-s11（跨调用保存）, ra, sp |
| **上下文切换** | 不需要专门保存                      | 必须在上下文切换中保存       |

---

### **2. 栈的切换**

**内核栈 vs 用户栈的管理**

**内核栈**：

每个进程有独立的内核栈

用于处理系统调用、中断和异常

在 `swtch` 中切换的是**内核栈指针**

**用户栈**：

每个进程有独立的用户栈

通过页表隔离，在用户态使用

在陷阱帧（trapframe）中保存和恢复

**切换时机**：

```c
// 用户态 → 内核态：保存用户栈到trapframe，切换到内核栈
// 内核态 → 用户态：从trapframe恢复用户栈
// 进程切换：在swtch中切换内核栈
```

**栈指针的保存和恢复**

```asm
# 保存当前栈指针
sd sp, 8(a0)        # 保存到old context的sp字段

# 恢复新进程栈指针  
ld sp, 8(a1)        # 从new context的sp字段恢复
```

**关键点**：`sp` 的切换意味着**整个栈环境的切换**，包括局部变量、返回地址等。

**栈溢出的检测和预防**

**检测机制**：

```c
// 在分配内核栈时设置保护页
#define KSTACK_GUARD_PAGES 1

// 栈溢出检查函数
void check_stack_overflow(struct proc *p) {
    uint64 stack_bottom = p->kstack;
    uint64 stack_top = p->kstack + PGSIZE;
    uint64 current_sp = r_sp();
    
    if (current_sp < stack_bottom + STACK_MARGIN || 
        current_sp >= stack_top) {
        panic("kernel stack overflow");
    }
}
```

**预防策略**：
1. 设置栈保护页（无映射页面）
2. 定期检查栈指针范围
3. 限制递归深度和栈分配大小

### **3. 上下文结构体设计**

```c
struct context {
    uint64 ra;   // 返回地址 - 决定切换后从哪里继续执行
    uint64 sp;   // 栈指针 - 切换栈环境
    
    // 被调用者保存寄存器
    uint64 s0;
    uint64 s1; 
    uint64 s2;
    uint64 s3;
    uint64 s4;
    uint64 s5;
    uint64 s6;
    uint64 s7;
    uint64 s8;
    uint64 s9;
    uint64 s10;
    uint64 s11;
    
    // 注意：不需要保存调用者保存寄存器(a0-a7, t0-t6)
    // 因为它们由C编译器在函数调用中自动处理
};
```

**寄存器选择理由**：

**ra, sp**：决定执行流和栈环境

**s0-s11**：包含重要的程序状态，需要跨函数调用保持不变

**省略其他**：编译器负责管理调用约定，临时寄存器无需保存

### **4. 上下文切换函数实现**

```c
// 上下文切换函数声明
void swtch(struct context *old, struct context *new);

// 使用示例
void scheduler_switch(struct proc *prev, struct proc *next) {
    // 切换到调度器上下文
    swtch(&prev->context, &next->context);
    // 当再次被调度时，从这里继续执行
}
```

### **5. 关键技术点实现**

**上下文切换必须是原子操作**

```c
// 在调用swtch前必须持有进程锁
void sched(void) {
    struct proc *p = myproc();
    
    if(!holding(&p->lock))
        panic("sched p->lock");
    
    // 关闭中断确保原子性
    int intena = mycpu()->intena;
    intr_off();
    
    swtch(&p->context, &mycpu()->context);
    
    // 恢复中断状态
    mycpu()->intena = intena;
}
```

**中断状态的管理**

```c
// 在上下文切换期间必须关闭中断
// 原因：
// 1. 防止切换过程中被中断打断
// 2. 避免中断处理程序看到不一致的进程状态
// 3. 确保切换操作的原子性
```

**多级栈的处理**

**三级栈架构**：

1. **用户栈**：用户态执行时使用，通过trapframe保存
2. **内核栈**：内核态执行时使用，每个进程独立
3. **中断栈**：某些系统为中断处理提供专用栈

**切换流程**：
```
用户进程A → 系统调用/中断 → 内核栈A → swtch → 内核栈B → 返回用户态 → 用户进程B
```

### **6. 完整的上下文切换流程**

```c
// 1. 进程A在内核态运行，决定让出CPU
yield() {
    acquire(&p->lock);
    p->state = RUNNABLE;
    sched();
}

// 2. 切换到调度器
sched() {
    intr_off();                    // 关闭中断
    swtch(&p->context, &cpu->scheduler_context);
    // 当再次被调度时继续执行
}

// 3. 调度器选择新进程
scheduler() {
    for(p = proc; p < &proc[NPROC]; p++) {
        if(p->state == RUNNABLE) {
            p->state = RUNNING;
            swtch(&cpu->scheduler_context, &p->context);
            // 当进程让出CPU时回到这里
        }
    }
}
```



## 任务5：实现调度器

### **1. scheduler() 函数分析**

**轮转调度的实现方式**

```c
void scheduler(void) {
    for(;;) {
        int found = 0;
        for(p = proc; p < &proc[NPROC]; p++) {
            acquire(&p->lock);
            if(p->state == RUNNABLE) {
                // 选择第一个找到的可运行进程
                p->state = RUNNING;
                c->proc = p;
                swtch(&c->context, &p->context);
                c->proc = 0;
                found = 1;
            }
            release(&p->lock);
        }
    }
}
```

**轮转调度特点**：

**简单遍历**：按进程表顺序扫描，选择第一个 `RUNNABLE` 进程

**无优先级**：所有进程平等对待

**无时间片**：进程运行直到主动放弃CPU（通过 `yield()`, `sleep()`, `exit()`）

**如何避免忙等待？**

```c
if(found == 0) {
    // nothing to run; stop running on this core until an interrupt.
    asm volatile("wfi");
}
```
**WFI指令**：当没有可运行进程时，执行 `wfi` (Wait For Interrupt) 让CPU进入低功耗状态

**中断唤醒**：当时钟中断、I/O完成等中断发生时，CPU恢复执行

**避免空转**：相比忙循环检查，显著降低CPU占用

**为什么需要开启中断？**

```c
intr_on();
intr_off();
```
**原因**：

1. **防止死锁**：如果所有进程都在等待I/O，但中断被关闭，I/O完成无法通知系统，导致永久等待
2. **设备响应**：允许磁盘、网络等设备通过中断通知操作完成
3. **时间片控制**：时钟中断是抢占调度的基础

### **2. 调度时机理解**

**主动调度 vs 抢占调度**

| 类型         | 触发方式     | 示例                           | 特点                    |
| ------------ | ------------ | ------------------------------ | ----------------------- |
| **主动调度** | 进程主动调用 | `yield()`, `sleep()`, `exit()` | 协作式，进程自愿放弃CPU |
| **抢占调度** | 时钟中断强制 | 时间片用完                     | 预防性，保证公平性      |

**yield() 函数的作用**

```c
void yield(void) {
    acquire(&p->lock);
    p->state = RUNNABLE;
    sched();
    release(&p->lock);
}
```
**主动让出CPU**：当前进程自愿放弃处理器

**状态转换**：`RUNNING` → `RUNNABLE`

**触发调度**：调用 `sched()` 切换到其他进程

**时钟中断如何触发调度**

```c
// 在时钟中断处理函数中
void timer_interrupt() {
    // 1. 更新计时器
    // 2. 检查当前进程运行时间
    if(current_process->running_time >= TIME_SLICE) {
        // 设置需要重新调度标志
        mycpu()->need_reschedule = 1;
    }
    
    // 3. 在中断返回前检查调度标志
    if(mycpu()->need_reschedule) {
        yield();  // 触发调度
    }
}
```

---

### **3. 调度器设计考虑**

**设计框架**

```c
void scheduler(void) {
    struct proc *p;
    struct cpu *c = mycpu();
    
    for(;;) {
        intr_on();  // 允许设备中断
        
        // ========== 调度算法核心 ==========
        struct proc *next = NULL;
        
        // 1. 选择下一个运行的进程
        next = select_next_process();
        
        if(next != NULL) {
            // 切换到选中的进程
            acquire(&next->lock);
            next->state = RUNNING;
            c->proc = next;
            swtch(&c->context, &next->context);
            c->proc = 0;
            release(&next->lock);
        } else {
            // 没有可运行进程，进入低功耗
            asm volatile("wfi");
        }
    }
}
```

### **四大设计问题的解决方案**

**1. 如何选择下一个运行的进程？**

**策略**：多级队列调度

```c
struct proc* select_next_process(void) {
    // 优先从高优先级队列选择
    for(int level = 0; level < MAX_PRIORITY_LEVELS; level++) {
        if(!is_queue_empty(priority_queues[level])) {
            return dequeue(priority_queues[level]);
        }
    }
    return NULL;  // 所有队列都为空
}
```

**2. 如何处理优先级？**

**方案**：动态优先级调整

```c
// 进程结构体扩展
struct proc {
    int static_priority;   // 静态优先级（用户设置）
    int dynamic_priority;  // 动态优先级（系统调整）
    int nice;             // 友好值（-20到19）
    uint64 wait_time;     // 等待时间
    uint64 run_time;      // 运行时间
};

// 优先级计算
int calculate_priority(struct proc *p) {
    return p->static_priority + p->nice + 
           (p->wait_time / TIME_UNIT) - 
           (p->run_time / TIME_UNIT);
}
```

**3. 如何避免饥饿？**

**机制**：老化（Aging）技术

```c
void aging(void) {
    for(each process p in wait queues) {
        p->wait_time++;
        // 长时间等待的进程提升优先级
        if(p->wait_time > AGING_THRESHOLD) {
            boost_priority(p);
        }
    }
}

// 定期调用老化（如在时钟中断中）
if(ticks % AGING_INTERVAL == 0) {
    aging();
}
```

**4. 如何平衡公平性和效率？**

**策略**：完全公平调度思想

```c
// 虚拟运行时间计算
uint64 vruntime = actual_runtime * (NICE_0_LOAD / weight);

// 选择vruntime最小的进程
struct proc* select_cfs_process(void) {
    struct proc *min_proc = NULL;
    uint64 min_vruntime = MAX_UINT64;
    
    for(each runnable process p) {
        if(p->vruntime < min_vruntime) {
            min_vruntime = p->vruntime;
            min_proc = p;
        }
    }
    return min_proc;
}
```

### **4. 扩展调度算法**

**优先级调度**

```c
// 基于优先级的调度框架
#define HIGH_PRIORITY   0
#define NORMAL_PRIORITY 1  
#define LOW_PRIORITY    2

struct priority_scheduler {
    struct proc *queues[3];  // 高、中、低优先级队列
    int policy;              // 调度策略
};

// 调度策略选项
enum {
    SCHED_FIFO,     // 先进先出
    SCHED_RR,       // 时间片轮转
    SCHED_OTHER     // 其他策略
};
```

**多级反馈队列（MLFQ）**

```c
#define MLFQ_LEVELS 5

struct mlfq_scheduler {
    struct proc *queues[MLFQ_LEVELS];
    int time_slices[MLFQ_LEVELS];  // 各队列时间片
    int boost_interval;            // 优先级提升间隔
};

// MLFQ规则：
// 1. 高优先级队列时间片短，低优先级队列时间长
// 2. 进程用完时间片降级到下一队列
// 3. 定期将所有进程提到最高队列（防止饥饿）
```

**完全公平调度器（CFS）**

```c
struct cfs_scheduler {
    struct rb_root tasks_tree;     // 红黑树，按vruntime排序
    uint64 min_vruntime;           // 最小虚拟运行时间
    int total_weight;              // 所有进程权重和
};

// CFS核心思想：
// 1. 每个进程有虚拟运行时间 vruntime
// 2. 总是选择vruntime最小的进程运行  
// 3. 根据进程优先级（权重）调整vruntime增长速率
// 4. 使用红黑树实现O(logN)的调度决策
```

### **5. 完整的调度器设计框架**

```c
// 调度器接口
void scheduler_init(void);
void schedule(void);
void enqueue_process(struct proc *p);
struct proc* dequeue_process(void);
void update_scheduler_stats(void);

// 进程调度相关信息
struct sched_info {
    uint64 create_time;
    uint64 total_runtime;
    uint64 last_scheduled;
    int priority;
    int times_scheduled;
};

// 调度策略选择
#ifdef CONFIG_SCHED_RR
    #define SCHEDULER round_robin_scheduler
#elif defined(CONFIG_SCHED_MLFQ)  
    #define SCHEDULER mlfq_scheduler
#elif defined(CONFIG_SCHED_CFS)
    #define SCHEDULER cfs_scheduler
#else
    #define SCHEDULER simple_scheduler
#endif
```



## 任务6：实现进程同步原语

### **1. sleep/wakeup 机制分析**

**sleep() 函数实现要点**

```c
void sleep(void *chan, struct spinlock *lk) {
    struct proc *p = myproc();
    
    // 必须获取进程锁来修改进程状态
    acquire(&p->lock);
    
    // 释放条件锁，允许其他进程修改条件
    release(lk);
    
    // 进入睡眠状态
    p->chan = chan;
    p->state = SLEEPING;
    
    // 切换到调度器
    sched();
    
    // 被唤醒后重新获取条件锁
    p->chan = 0;
    release(&p->lock);
    acquire(lk);
}
```

**wakeup() 函数实现要点**

```c
void wakeup(void *chan) {
    struct proc *p;
    
    for(p = proc; p < &proc[NPROC]; p++) {
        if(p != myproc()) {
            acquire(&p->lock);
            if(p->state == SLEEPING && p->chan == chan) {
                p->state = RUNNABLE;
            }
            release(&p->lock);
        }
    }
}
```

### **2. 条件变量的概念**

**条件变量语义**

```c
// 条件变量的抽象接口
struct condvar {
    void *chan;          // 等待通道标识
    struct spinlock lock; // 保护条件变量的锁
};

void cond_wait(struct condvar *cv, struct spinlock *lk) {
    sleep(cv->chan, lk);
}

void cond_signal(struct condvar *cv) {
    wakeup(cv->chan);
}

void cond_broadcast(struct condvar *cv) {
    wakeup(cv->chan);  // xv6中wakeup会唤醒所有等待者
}
```

**使用模式**

```c
// 等待条件
acquire(&lock);
while(condition_is_false) {
    sleep(channel, &lock);
}
// 条件满足，执行操作
do_something();
release(&lock);

// 通知条件
acquire(&lock);
make_condition_true();
wakeup(channel);
release(&lock);
```

### **3. 典型使用模式分析**

#### **a. 生产者-消费者问题**
```c
#define N 100

struct buffer {
    int data[N];
    int head;      // 读位置
    int tail;      // 写位置  
    int count;     // 当前元素数
    struct spinlock lock;
};

// 生产者
void producer(struct buffer *b, int item) {
    acquire(&b->lock);
    
    while(b->count == N) {
        // 缓冲区满，等待消费者消费
        sleep(b, &b->lock);  // 使用buffer指针作为channel
    }
    
    b->data[b->tail] = item;
    b->tail = (b->tail + 1) % N;
    b->count++;
    
    // 唤醒可能等待的消费者
    wakeup(b);
    
    release(&b->lock);
}

// 消费者
int consumer(struct buffer *b) {
    acquire(&b->lock);
    
    while(b->count == 0) {
        // 缓冲区空，等待生产者生产
        sleep(b, &b->lock);
    }
    
    int item = b->data[b->head];
    b->head = (b->head + 1) % N;
    b->count--;
    
    // 唤醒可能等待的生产者
    wakeup(b);
    
    release(&b->lock);
    return item;
}
```

#### **b. 读者-写者问题**
```c
struct rwlock {
    int readers;        // 当前读者数
    int writing;        // 是否有写者
    struct spinlock lock;
};

// 读者
void reader_acquire(struct rwlock *rw) {
    acquire(&rw->lock);
    
    while(rw->writing) {
        // 有写者正在写，等待
        sleep(rw, &rw->lock);
    }
    
    rw->readers++;
    release(&rw->lock);
}

void reader_release(struct rwlock *rw) {
    acquire(&rw->lock);
    rw->readers--;
    if(rw->readers == 0) {
        // 没有读者了，唤醒可能等待的写者
        wakeup(rw);
    }
    release(&rw->lock);
}

// 写者
void writer_acquire(struct rwlock *rw) {
    acquire(&rw->lock);
    
    while(rw->writing || rw->readers > 0) {
        // 有写者或读者，等待
        sleep(rw, &rw->lock);
    }
    
    rw->writing = 1;
    release(&rw->lock);
}

void writer_release(struct rwlock *rw) {
    acquire(&rw->lock);
    rw->writing = 0;
    // 唤醒所有等待的读者和写者
    wakeup(rw);
    release(&rw->lock);
}
```

#### **c. 信号量的实现**
```c
struct semaphore {
    int count;              // 信号量值
    struct spinlock lock;
};

void sem_init(struct semaphore *sem, int value) {
    sem->count = value;
    initlock(&sem->lock, "semaphore");
}

void sem_wait(struct semaphore *sem) {
    acquire(&sem->lock);
    
    while(sem->count <= 0) {
        // 资源不可用，等待
        sleep(sem, &sem->lock);
    }
    
    sem->count--;
    release(&sem->lock);
}

void sem_post(struct semaphore *sem) {
    acquire(&sem->lock);
    
    sem->count++;
    // 唤醒可能等待的进程
    wakeup(sem);
    
    release(&sem->lock);
}
```

### **4. 实现要点详解**


**（1）避免lost wakeup问题**

**问题描述**：在检查条件和调用sleep之间的时间窗口内，条件可能已经改变，但进程仍然进入睡眠，导致唤醒丢失。

**xv6的解决方案**：

```c
// 正确的模式
acquire(&lock);
while(condition == false) {  // 使用while而不是if
    sleep(channel, &lock);   // 在持有进程锁的情况下修改状态
}
// 条件满足
release(&lock);

// 错误的模式（会导致lost wakeup）
if(condition == false) {     // 检查条件
    release(&lock);          // 释放锁
    sleep(channel);          // 进入睡眠 - 这里可能丢失唤醒!
    acquire(&lock);
}
```

**关键保证**：`sleep()` 函数内部在修改进程状态前持有进程锁，确保wakeup不会在状态修改过程中发生。

**（2）锁的正确使用**

**双锁机制**：
```c
void sleep(void *chan, struct spinlock *lk) {
    // lk: 条件锁（保护共享数据）
    // &p->lock: 进程锁（保护进程状态）
    
    acquire(&p->lock);  // 1. 获取进程锁
    release(lk);        // 2. 释放条件锁（允许其他进程修改条件）
    
    p->chan = chan;
    p->state = SLEEPING;
    sched();            // 3. 切换时仍然持有进程锁
    
    // 被唤醒后
    release(&p->lock);  // 4. 释放进程锁
    acquire(lk);        // 5. 重新获取条件锁
}
```

**锁的顺序规则**：
1. 先获取条件锁，再获取进程锁
2. 在sleep中先释放条件锁，再获取进程锁
3. 确保不会出现死锁

**（3）中断状态的管理**

**中断安全**：
```c
void sleep(void *chan, struct spinlock *lk) {
    // 在调用sleep前可能持有自旋锁，需要处理中断状态
    
    // 保存中断状态
    int intena = mycpu()->intena;
    
    acquire(&p->lock);
    release(lk);
    
    // 进入睡眠
    sched();
    
    // 恢复中断状态
    mycpu()->intena = intena;
    
    release(&p->lock);
    acquire(lk);
}
```

**中断处理中的同步**：
```c
// 中断处理程序中唤醒进程
void interrupt_handler(void) {
    acquire(&condition_lock);
    // 修改共享条件
    condition = true;
    wakeup(channel);
    release(&condition_lock);
}
```

### **5. 高级同步原语扩展**

**计数信号量**

```c
struct counting_semaphore {
    int value;
    struct spinlock lock;
};

void sem_wait(struct counting_semaphore *sem, int n) {
    acquire(&sem->lock);
    while(sem->value < n) {
        sleep(sem, &sem->lock);
    }
    sem->value -= n;
    release(&sem->lock);
}

void sem_post(struct counting_semaphore *sem, int n) {
    acquire(&sem->lock);
    sem->value += n;
    wakeup(sem);
    release(&sem->lock);
}
```

**屏障同步**

```c
struct barrier {
    int total;          // 需要等待的总进程数
    int waiting;        // 当前等待的进程数
    int generation;     // 代际编号（用于区分不同的等待轮次）
    struct spinlock lock;
};

void barrier_wait(struct barrier *b) {
    acquire(&b->lock);
    
    int my_generation = b->generation;
    b->waiting++;
    
    if(b->waiting == b->total) {
        // 最后一个进程到达，唤醒所有进程
        b->waiting = 0;
        b->generation++;
        wakeup(b);
    } else {
        // 等待其他进程
        while(my_generation == b->generation) {
            sleep(b, &b->lock);
        }
    }
    
    release(&b->lock);
}
```

**监控器（Monitor）模式**

```c
struct monitor {
    struct spinlock lock;
    struct condvar cond;
};

void monitor_enter(struct monitor *m) {
    acquire(&m->lock);
}

void monitor_exit(struct monitor *m) {
    release(&m->lock);
}

void monitor_wait(struct monitor *m) {
    cond_wait(&m->cond, &m->lock);
}

void monitor_signal(struct monitor *m) {
    cond_signal(&m->cond);
}
```





## 测试结果

### 1、进程创建测试

#### **测试代码**

```
// 进程创建测试
void test_process_creation(void) {
    printf("Testing process creation...\n");
    
    // 先清理
    int status;
    while (wait_process(&status) > 0) {
        // 清理僵尸进程
    }
    
    // 测试基本的进程创建
    int pid = create_process(simple_task);
    printf("Created process with PID: %d\n", pid);
    
    // 运行这个进程
    run_processes();
    
    // 等待进程完成
    wait_process(NULL);
    
    // 测试进程表限制
    int pids[NPROC];
    int count = 0;
    
    printf("Testing process table limits...\n");
    for (int i = 0; i < NPROC + 5; i++) {
        int pid = create_process(simple_task);
        if (pid > 0) {
            pids[count++] = pid;
            printf("Created process %d\n", pid);
        } else {
            printf("Failed to create process (table full at %d processes)\n", count);
            break;
        }
    }
    
    printf("Created %d processes\n", count);
    
    // 运行所有进程
    printf("Running all processes...\n");
    run_processes();
    
    // 清理测试进程
    for (int i = 0; i < count; i++) {
        int status;
        wait_process(&status);
        printf("Process %d exited with status %d\n", pids[i], status);
    }
    
    printf("Process creation test completed\n\n");
}
```

#### **输出结果**

![image-20251109172437841](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109172437841.png)

![image-20251109172501769](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109172501769.png)

![image-20251109172524285](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109172524285.png)

![image-20251109172535662](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109172535662.png)

![image-20251109172546551](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109172546551.png)

#### 结果分析：

（1） **进程创建功能** 

成功创建了33个进程（PID 1-33）

进程表限制正确：NPROC=32，第33个创建失败

所有进程都正确分配了独立的内核栈

（2）**调度器功能** 

所有32个进程都按顺序被调度执行

上下文切换完全正确：

​	每个进程有独立的上下文结构（地址递增）

​	每个进程有独立的栈空间（地址递减）

​	调度器上下文固定（old=0x80005588）

（3）**进程生命周期管理** 

创建 → 运行 → 退出 → 回收 完整流程

所有进程正常执行`simple_task`并退出

僵尸进程正确回收



### 2、调度器测试

#### 测试代码

```
// 调度器测试
void test_scheduler(void) {
    printf("Testing scheduler...\n");
    
    // 先清理之前的进程
    int status;
    while (wait_process(&status) > 0) {
        printf("Cleaned up zombie process\n");
    }
    
    // 创建多个计算密集型进程
    for (int i = 0; i < 3; i++) {
        create_process(cpu_intensive_task);
    }
    
    // 运行这些进程
    printf("Running multiple processes...\n");
    run_processes();
    
    // 等待所有进程完成
    for (int i = 0; i < 3; i++) {
        wait_process(&status);
        printf("Process completed with status: %d\n", status);
    }
    
    printf("Scheduler test completed\n\n");
}
```

#### 输出结果

![image-20251109173013261](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109173013261.png)

![image-20251109173027814](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109173027814.png)

![image-20251109173038515](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109173038515.png)

#### 结果分析

（1）**调度行为** 

创建了3个CPU密集型进程（PID 1-3）

**顺序执行**：进程1 → 进程2 → 进程3

每个进程都**完整运行完毕**后才切换到下一个

（2）**调度策略**

**FIFO + 协作式**

进程按创建顺序执行

需要进程主动让出CPU

（3） **技术实现验证** 

**上下文切换正确**：每个进程有独立上下文和栈

**内存隔离良好**：栈空间不重叠

**进程管理完善**：创建、运行、退出、回收全流程正常



### 3、同步机制测试

#### 测试代码

```
// 同步机制测试
void test_synchronization(void) {
    printf("Testing synchronization...\n");
    
    // 先清理之前的进程
    int status;
    while (wait_process(&status) > 0) {
        printf("Cleaned up zombie process\n");
    }
    
    // 测试生产者-消费者场景
    shared_buffer_init();
    create_process(producer_task);
    create_process(consumer_task);
    
    // 运行这些进程
    printf("Running producer-consumer processes...\n");
    run_processes();
    
    // 等待完成
    wait_process(NULL);
    wait_process(NULL);
    
    printf("Synchronization test completed\n\n");
}
```

#### 输出结果

![image-20251109173520174](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109173520174.png)

![image-20251109173530489](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109173530489.png)

#### 结果分析

（1） **执行顺序**

生产者: 启动 → 生产5个item → 完成
消费者: 启动 → 消费5个item → 完成

**顺序执行**而非并发：

生产者先运行完成所有生产

消费者后运行完成所有消费

（2）**同步机制验证** 

**数据完整性**：生产5个，消费5个，无丢失无重复

**缓冲区管理**：in/out指针正确工作

**进程协调**：两个进程正常协作完成工作

（3） **技术实现正确** 

进程创建和调度正常

上下文切换完美

资源共享无冲突



## 扩展调度算法：优先级调度

#### 代码

```
// kernel/priority.c - 优先级调度扩展
#include "proc.h"
#include "printf.h"
#include "clock.h"
#include "priority.h"

static struct context scheduler_context; // 调度器自身的上下文
// 添加缺少的函数声明
extern void context_switch(struct context *old, struct context *new);
extern uint64_t get_ticks(timer_type_t timer);

// 优先级定义
#define PRIORITY_MIN 1
#define PRIORITY_MAX 10
#define PRIORITY_DEFAULT 5

// 进程控制块扩展 - 添加优先级字段
struct proc_priority {
    struct proc *proc;
    int priority;
    int dynamic_priority;  // 动态优先级（用于反馈调度）
    uint64_t run_time;     // 累计运行时间
    uint64_t wait_time;    // 累计等待时间
    int nice;              // 友好值（-20 到 19）
};

static struct proc_priority priority_procs[NPROC];
static int priority_initialized = 0;

// 获取进程优先级
int get_priority(int pid) {
    spin_lock(&proc_lock);
    
    for (int i = 0; i < NPROC; i++) {
        if (priority_procs[i].proc->pid == pid) {
            int priority = priority_procs[i].priority;
            spin_unlock(&proc_lock);
            return priority;
        }
    }
    
    spin_unlock(&proc_lock);
    return -1;
}

// 获取进程动态优先级
int get_dynamic_priority(int pid) {
    spin_lock(&proc_lock);
    
    for (int i = 0; i < NPROC; i++) {
        if (priority_procs[i].proc->pid == pid) {
            int dpriority = priority_procs[i].dynamic_priority;
            spin_unlock(&proc_lock);
            return dpriority;
        }
    }
    
    spin_unlock(&proc_lock);
    return -1;
}

// 优先级调度初始化
void priority_init(void) {
    printf("Priority: initializing priority scheduler\n");
    
    for (int i = 0; i < NPROC; i++) {
        priority_procs[i].proc = &proc[i];
        priority_procs[i].priority = PRIORITY_DEFAULT;
        priority_procs[i].dynamic_priority = PRIORITY_DEFAULT;
        priority_procs[i].run_time = 0;
        priority_procs[i].wait_time = 0;
        priority_procs[i].nice = 0;
    }
    
    priority_initialized = 1;
    printf("Priority: scheduler initialized with %d priority levels\n", 
           PRIORITY_MAX - PRIORITY_MIN + 1);
}

// 设置进程优先级
int set_priority(int pid, int priority) {
    if (!priority_initialized) {
        priority_init();
    }
    
    if (priority < PRIORITY_MIN || priority > PRIORITY_MAX) {
        printf("Priority: invalid priority %d (must be %d-%d)\n", 
               priority, PRIORITY_MIN, PRIORITY_MAX);
        return -1;
    }
    
    spin_lock(&proc_lock);
    
    for (int i = 0; i < NPROC; i++) {
        if (priority_procs[i].proc->pid == pid) {
            priority_procs[i].priority = priority;
            priority_procs[i].dynamic_priority = priority;
            printf("Priority: set process %d priority to %d\n", pid, priority);
            spin_unlock(&proc_lock);
            return 0;
        }
    }
    
    spin_unlock(&proc_lock);
    printf("Priority: process %d not found\n", pid);
    return -1;
}

// 设置进程友好值
int set_nice(int pid, int nice) {
    if (nice < -20 || nice > 19) {
        printf("Priority: invalid nice value %d (must be -20 to 19)\n", nice);
        return -1;
    }
    
    spin_lock(&proc_lock);
    
    for (int i = 0; i < NPROC; i++) {
        if (priority_procs[i].proc->pid == pid) {
            priority_procs[i].nice = nice;
            // 根据nice值调整优先级
            int new_priority = PRIORITY_DEFAULT + (nice / 4); // 简单映射
            if (new_priority < PRIORITY_MIN) new_priority = PRIORITY_MIN;
            if (new_priority > PRIORITY_MAX) new_priority = PRIORITY_MAX;
            
            priority_procs[i].priority = new_priority;
            printf("Priority: set process %d nice to %d, priority to %d\n", 
                   pid, nice, new_priority);
            spin_unlock(&proc_lock);
            return 0;
        }
    }
    
    spin_unlock(&proc_lock);
    printf("Priority: process %d not found\n", pid);
    return -1;
}

static struct proc* get_highest_priority_proc(void) {
    struct proc *selected = NULL;
    int highest_priority = -1;
    
    printf("get_highest_priority_proc: starting search through %d process slots\n", NPROC);
    
    for (int i = 0; i < NPROC; i++) {
        
        // 直接检查进程状态，不依赖 priority_procs
        if (proc[i].state == RUNNABLE) {
            
            // 获取优先级
            int priority = -1;
            for (int j = 0; j < NPROC; j++) {
                if (priority_procs[j].proc == &proc[i]) {
                    priority = priority_procs[j].dynamic_priority;
                    break;
                }
            }
            
            
            if (priority > highest_priority) {
                highest_priority = priority;
                selected = &proc[i];
            }
        } else if (proc[i].state != UNUSED) {
        } else {
        }
    }
    
    if (selected) {
        printf("get_highest_priority_proc: FINAL SELECTION - process %d with priority %d\n", 
               selected->pid, highest_priority);
    } else {
        printf("get_highest_priority_proc: NO RUNNABLE PROCESSES FOUND\n");
    }
    
    return selected;
}

// 更新动态优先级（简单的反馈机制）
// static void update_dynamic_priority(struct proc *p, uint64_t runtime) {
//     for (int i = 0; i < NPROC; i++) {
//         if (priority_procs[i].proc == p) {
//             // 增加运行时间
//             priority_procs[i].run_time += runtime;
            
//             // 简单的老化机制：长时间等待的进程提高优先级
//             if (priority_procs[i].wait_time > 1000000) { // 阈值
//                 if (priority_procs[i].dynamic_priority < PRIORITY_MAX) {
//                     priority_procs[i].dynamic_priority++;
//                 }
//                 priority_procs[i].wait_time = 0;
//             }
            
//             // 减少运行时间长的进程的优先级（防止饥饿）
//             if (priority_procs[i].run_time > 5000000) { // 阈值
//                 if (priority_procs[i].dynamic_priority > PRIORITY_MIN) {
//                     priority_procs[i].dynamic_priority--;
//                 }
//                 priority_procs[i].run_time = 0;
//             }
//             break;
//         }
//     }
// }

// 增加所有可运行进程的等待时间
// static void increment_wait_time(void) {
//     for (int i = 0; i < NPROC; i++) {
//         if (priority_procs[i].proc->state == RUNNABLE) {
//             priority_procs[i].wait_time++;
//         }
//     }
// }

void priority_scheduler(void) {
    // static uint64_t last_schedule_time = 0;
    static int scheduler_started = 0;
    static int iteration_count = 0;
    
    if (!priority_initialized) {
        priority_init();
    }
    
    if (!scheduler_started) {
        printf("Priority: starting priority-based scheduler\n");
        scheduler_started = 1;
    }
    
    printf("Priority: entering main scheduler loop\n");
    
    for (;;) {
        iteration_count++;
        printf("Priority: scheduler iteration %d\n", iteration_count);
        
        // 安全限制
        if (iteration_count > 10) {
            printf("EMERGENCY: Scheduler iteration limit reached, returning\n");
            return;
        }
        
        asm volatile("csrs mstatus, %0" : : "r" (1 << 3));
        
        printf("Priority: looking for runnable processes...\n");
        
        spin_lock(&proc_lock);
        struct proc *p = get_highest_priority_proc();
        
        if (p) {
            printf("Priority: selected process %d for execution\n", p->pid);
            
            p->state = RUNNING;
            struct proc *prev_proc = curr_proc;
            curr_proc = p;
            
            spin_unlock(&proc_lock);
            
            printf("Priority: BEFORE context_switch\n");
            
            if (prev_proc) {
                context_switch(&prev_proc->context, &p->context);
            } else {
                context_switch(&scheduler_context, &p->context);
            }
            
            // 当上下文切换返回时，进程已经执行完毕或被抢占
            printf("Priority: AFTER context_switch - returned from process\n");
            
            // 重新获取锁来更新状态
            spin_lock(&proc_lock);
            
            // 如果进程还在RUNNING状态，说明它被抢占了，设置回RUNNABLE
            if (p->state == RUNNING) {
                printf("Priority: process %d was preempted, setting to RUNNABLE\n", p->pid);
                p->state = RUNNABLE;
            }
            // 如果进程已经设置为ZOMBIE（通过任务函数），我们保持那个状态
            
            curr_proc = 0;
            spin_unlock(&proc_lock);
            
        } else {
            printf("Priority: no runnable processes found\n");
            spin_unlock(&proc_lock);
            printf("Priority: returning from scheduler\n");
            return;
        }
        
        // 添加检查：如果所有进程都完成了，提前退出
        int all_completed = 1;
        spin_lock(&proc_lock);
        for (int i = 0; i < NPROC; i++) {
            if (proc[i].state == RUNNABLE || proc[i].state == RUNNING) {
                all_completed = 0;
                break;
            }
        }
        spin_unlock(&proc_lock);
        
        if (all_completed) {
            printf("Priority: all processes completed, returning from scheduler\n");
            return;
        }
    }
}

// 显示所有进程的优先级信息
void show_priority_info(void) {
    printf("\n=== Priority Scheduling Information ===\n");
    
    spin_lock(&proc_lock);
    
    for (int i = 0; i < NPROC; i++) {
        if (priority_procs[i].proc->state != UNUSED) {
            printf("Process %d: state=%d, priority=%d, dynamic=%d, nice=%d\n",
                   priority_procs[i].proc->pid,
                   priority_procs[i].proc->state,
                   priority_procs[i].priority,
                   priority_procs[i].dynamic_priority,
                   priority_procs[i].nice);
        }
    }
    
    spin_unlock(&proc_lock);
    printf("=======================================\n\n");
}

// 在 proc.c 中修改 exit_process
void exit_process2(int status) {
    if (curr_proc == 0) {
        printf("Process: no current process to exit\n");
        return;
    }
    
    printf("Process %d: exiting with status %d\n", curr_proc->pid, status);
    
    curr_proc->xstate = status;
    curr_proc->state = ZOMBIE;
    curr_proc->killed = 0;
    
    // 唤醒父进程
    if (curr_proc->parent) {
        wakeup(curr_proc->parent);
    }
    
    printf("Process %d: switching back to scheduler\n", curr_proc->pid);
    
    struct proc *p = curr_proc;
    curr_proc = 0;
    
    // 切换到调度器上下文
    context_switch(&p->context, &scheduler_context);
    
    // 这行代码不应该执行
    printf("ERROR: exit_process continued after context switch!\n");
    for (;;) { asm volatile("wfi"); }
}

void high_priority_task(void) {
    if (!curr_proc) {
        printf("ERROR: high_priority_task - no current process!\n");
        return;
    }
    
    int pid = curr_proc->pid;
    printf("🎯 HIGH PRIORITY Process %d: TASK STARTED\n", pid);
    
    for (int i = 0; i < 3; i++) {
        printf("HIGH PRIORITY %d: working step %d/3\n", pid, i + 1);
        for (volatile int j = 0; j < 10000; j++);
    }
    
    printf("✅ HIGH PRIORITY Process %d: TASK COMPLETED - calling exit\n", pid);
    
    // 使用修复后的 exit_process
    exit_process2(0);
    
    // 不应该到达这里
    printf("ERROR: Continued after exit!\n");
    for (;;) { asm volatile("wfi"); }
}

// 优先级测试任务 - 中优先级
void medium_priority_task(void) {
    int pid = curr_proc->pid;
    printf("➡️ MEDIUM PRIORITY Process %d: STARTED\n", pid);
    
    for (int i = 0; i < 8; i++) {
        printf("MEDIUM PRIORITY %d: working at normal speed... step %d\n", pid, i + 1);
        for (volatile int j = 0; j < 200000; j++); // 中等延时
    }
    
    printf("✅ MEDIUM PRIORITY Process %d: COMPLETED\n", pid);
    exit_process2(0);
}

// 优先级测试任务 - 低优先级
void low_priority_task(void) {
    int pid = curr_proc->pid;
    printf("🐢 LOW PRIORITY Process %d: STARTED\n", pid);
    
    for (int i = 0; i < 10; i++) {
        printf("LOW PRIORITY %d: working slowly... step %d\n", pid, i + 1);
        for (volatile int j = 0; j < 300000; j++); // 长延时
    }
    
    printf("✅ LOW PRIORITY Process %d: COMPLETED\n", pid);
    exit_process2(0);
}

// CPU密集型任务（用于测试优先级影响）
void cpu_intensive_priority_task(void) {
    int pid = curr_proc->pid;
    int priority = get_priority(pid);
    printf("Process %d (priority %d): CPU intensive task started\n", pid, priority);
    
    uint64_t computations = 0;
    for (uint64_t i = 0; i < 500000; i++) {
        computations += i * i;
        if (i % 50000 == 0) {
            printf("Process %d (priority %d): computation progress %lu\n", 
                   pid, priority, i);
        }
    }
    
    printf("Process %d (priority %d): completed, result=%lu\n", 
           pid, priority, computations);
    exit_process2(0);
}
```

#### 算法核心逻辑原理

**（1）核心数据结构**

进程优先级控制块

```c
struct proc_priority {
    struct proc *proc;           // 指向基础进程控制块
    int priority;               // 静态优先级 (1-10)
    int dynamic_priority;       // 动态优先级 (用于反馈调度)
    uint64_t run_time;          // 累计运行时间
    uint64_t wait_time;         // 累计等待时间  
    int nice;                   // 友好值 (-20 到 19)
};
```

**（2）调度算法核心逻辑**

进程选择策略 - `get_highest_priority_proc()`

**关键特点**：遍历所有进程，选择动态优先级最高的RUNNABLE进程，时间复杂度：O(N)，N为进程数量

优先级映射机制

静态优先级设置

```c
// 直接设置优先级 (1-10)
set_priority(pid, priority);
```

Nice值映射

```c
// 通过nice值间接设置优先级
new_priority = PRIORITY_DEFAULT + (nice / 4);
// 映射关系：nice值每变化4，优先级变化1
```

**（3）调度器工作流程**

3.1 主调度循环 - `priority_scheduler()`

循环:
  1. 开启中断
  2. 获取进程锁
  3. 调用 get_highest_priority_proc() 选择进程
  4. 如果找到进程:
     
      ​	设置进程状态为RUNNING
      
      ​	设置当前进程指针
      
      ​	释放锁
      
      ​	执行上下文切换
      
      ​	进程执行完成后返回
      
      ​	重新获取锁，更新进程状态
  5. 如果没找到进程:
     
      ​	检查是否所有进程都完成
      
      ​	是则返回，否则继续循环

3.2 上下文切换机制

// 从当前进程切换到目标进程
context_switch(&当前进程上下文, &目标进程上下文);

// 进程退出时切换回调度器  
context_switch(&退出进程上下文, &scheduler_context);

#### 可能的优化：
1. **优先级队列** - 使用堆结构将复杂度降至O(logN)
2. **多级反馈队列** - 结合时间片轮转
3. **实时调度** - 支持截止时间约束
4. **负载均衡** - 多核环境下的优先级调度

#### 输出结果

![image-20251109200751201](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109200751201.png)

![image-20251109200821365](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109200821365.png)

![image-20251109200845953](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109200845953.png)

![image-20251109200903331](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251109200903331.png)



**进程创建和优先级设置**

Process 1: priority 9 (高优先级I/O任务)
Process 2: priority 3 (低优先级CPU任务)  
Process 3: priority 4 (CPU任务)
Process 4: priority 6 (中等优先级任务)

**调度顺序**

执行顺序: 进程1(9) → 进程4(6) → 进程3(4) → 进程2(3)

调度器首先选择了**优先级最高**的进程1（优先级9），然后按**优先级降序**执行：6 → 4 → 3

