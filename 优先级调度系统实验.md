# 扩展实验一：优先级调度系统

## 一、实验过程分析

### 1. 原有调度机制分析
xv6采用简单的Round-Robin调度算法，存在以下问题：

**（1）响应性不佳**：交互任务与CPU密集型任务竞争时延迟显著

**（2）无优先级概念**：无法区分重要任务与后台任务

**（3）缺乏动态调整**：不支持根据进程行为动态调整调度策略

### 2. 优先级调度实现难点
**并发控制**：在多核环境下，进程状态变化和调度决策需要严格的锁保护
```c
acquire(&ptable.lock);
// 调度决策和状态修改
release(&ptable.lock);
```

**上下文保存**：确保调度过程中寄存器状态正确保存和恢复
```c
// kernel/switch.S 中的上下文切换
swtch:
    sd ra, 0(a0)
    sd sp, 8(a0)
    sd s0, 16(a0)
    // ... 保存其他寄存器
```

### 3.优先级调度的核心实现
**（1）代码结构分析**

研究xv6原有调度框架，重点分析：

`kernel/proc.c`中的`scheduler()`函数

`struct proc`进程结构体定义

上下文切换机制（`kernel/switch.S`）

**（2）优先级调度实现**

**进程结构扩展**（`include/proc.h`）：

![image-20251201101330879](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201101330879.png)

![image-20251201101350144](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201101350144.png)

**调度器修改**（`kernel/proc.c`）：

![image-20251201101753868](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201101753868.png)

​	上述代码实现了一个**最高优先级进程选择算法**，它通过遍历进程表，在可运行（RUNNABLE）的进程中按照**优先级最高→等待时间最长→已运行时间最短→进程ID最小**的层级规则来选择最优进程，确保高优先级任务优先执行的同时兼顾公平性和资源平衡。

**（3）系统调用实现**

![image-20251201103531963](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201103531963.png)

添加`setpriority`和`getpriority`系统调用，支持用户态优先级调整。

**（4） Aging 机制设计**（kernel/proc.c)

![image-20251201103931212](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201103931212.png)

​	上述代码实现了**Aging防饥饿机制**：它定期遍历所有可运行或睡眠状态的进程，递增其等待时间，当任一进程的等待时间超过预设阈值时，就通过`mlfq_promote()`将其提升到更高优先级队列并重置等待时间，从而确保**长时间等待的进程不会因优先级过低而永远无法获得CPU时间**，有效防止进程饥饿问题。

### 4、扩展功能：多级反馈调度（MLFQ）

不仅区分优先级，还根据 CPU 行为动态调整 

进程使用时间片多 → 降级 

等待时间长 → 升级 

常用于交互型系统

**(1）队列配置**：

![image-20251201101930478](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201101930478.png)

​	上述代码定义了**多级反馈队列（MLFQ）的核心参数**：`mlfq_time_slices`数组设置了三个队列层级的时间片配额（1、2、4个时间单位），层级越高时间片越长；`mlfq_level_priorities`数组则定义了对应队列的优先级映射（最高、默认、略高于最低优先级），共同实现了**高层级队列响应快但时间片短、低层级队列优先级低但时间片长**的调度策略。

**（2）降级逻辑**：

![image-20251201102106095](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201102106095.png)

​	上述代码实现了**多级反馈队列的降级机制**：当一个进程`p`在当前队列层级耗尽时间片后，如果它不在最低层级，就会将其下移到下一个更低优先级的队列（`queue_level++`），同时重置该队列的时间片计数（`queue_ticks = 0`）并更新对应的优先级映射，从而让**CPU密集型任务逐步降低调度优先级**，避免长时间占用系统资源。

**（3）升级逻辑**：

![image-20251201102249019](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201102249019.png)

​	上述代码实现了**多级反馈队列的升级机制**：当一个进程`p`长时间处于等待状态（如I/O阻塞）后，如果它不在最高层级队列，就会将其提升到上一个更高优先级的队列（`queue_level--`），同时重置时间片计数并更新优先级映射，从而让**交互型或I/O密集型任务能够获得更及时的响应**，并通过日志输出记录升级过程以便调试观察。

## 二、实验结果总结

### 1、实验过程中遇到的问题

**（1）死锁风险**：在持有进程锁的情况下调用调度函数

**（2）状态不一致**：进程退出时资源清理不彻底

**（3）性能瓶颈**：全表扫描选择最高优先级进程

### 2、输出结果
**（1）T1：两个任务，优先级差距大，高优先级任务先执行完。**

**测试代码**

```
static void test_priority_gap(void) {
    printf("\n[T1] 高低优先级对比（高优先级应先完成）\n");
    cleanup_all_processes();
    
    int fast = create_process(high_priority_worker);
    int slow = create_process(low_priority_worker);
    set_priority(fast, PRIORITY_MAX);
    set_priority(slow, PRIORITY_MIN + 1);
    
    drive_scheduler_until_idle();
    
    struct completion_log log;
    collect_completion(&log, 2);
    print_completion("T1", &log);
    
    if (log.count >= 1 && log.order[0] == fast) {
        printf("  [PASS] 高优先级进程 %d 最先完成\n", fast);
    } else {
        printf("  [FAIL] 期望 %d 先完成，但实际顺序不同\n", fast);
    }
    
    cleanup_all_processes();
}
```

**输出结果**

![image-20251201104842772](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201104842772.png)

![image-20251201104855725](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201104855725.png)

（1）初始优先级设置：

进程1：优先级10（最高）

进程2：优先级2（较低）

（2）调度行为分析：

前3个时间片：进程1连续获得CPU（优先级10→5的变化显示MLFQ在调整）

第4个时间片：进程2首次获得调度（此时两进程优先级均为2）

后续调度：两进程进入轮转模式，交替执行

（3）MLFQ动态调整：

进程1从初始优先级10快速降至2，说明它是CPU密集型任务

进程2保持优先级2，与进程1形成同级竞争

降级机制有效防止高优先级进程垄断CPU

（4）完成顺序验证：

进程1先完成（ticks=10时退出）

进程2后完成（ticks=8时仍在运行，后续退出）



**（2）T2：相同优先级，行为等价RR**

**测试代码**

```
static void test_equal_priority_rr(void) {
    printf("\n[T2] 相同优先级任务公平性（应近似 RR）\n");
    cleanup_all_processes();
    
    int pids[3];
    for (int i = 0; i < 3; i++) {
        pids[i] = create_process(rr_worker);
        set_priority(pids[i], PRIORITY_DEFAULT);
    }
    
    drive_scheduler_until_idle();
    
    int ticks[3];
    for (int i = 0; i < 3; i++) {
        ticks[i] = snapshot_ticks(pids[i]);
    }
    
    struct completion_log log;
    collect_completion(&log, 3);
    print_completion("T2", &log);
    
    int min = ticks[0], max = ticks[0];
    for (int i = 1; i < 3; i++) {
        if (ticks[i] < min) min = ticks[i];
        if (ticks[i] > max) max = ticks[i];
    }
    
    printf("  调度次数 ticks: %d %d %d\n", ticks[0], ticks[1], ticks[2]);
    if (max - min <= 1) {
        printf("  [PASS] 调度次数接近，表现与 RR 类似\n");
    } else {
        printf("  [WARN] 调度次数差异较大，可进一步检查\n");
    }
    
    cleanup_all_processes();
}
```

**输出结果**

![image-20251201105452523](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201105452523.png)

![image-20251201105509822](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201105509822.png)

（1）初始条件设置：

进程3、4、5：相同优先级5

三个进程具有相同的入口地址和代码逻辑

（2）调度模式分析：

完美轮转序列：3→4→5→3→4→5→3→4→5...

严格循环顺序：每个进程获得完全相等的调度机会

无优先级干扰：所有进程始终保持相同优先级



**（3）T3：高低混合 + aging，所有任务最终执行完**

**测试代码**

```
static void test_aging_prevents_starvation(void) {
    printf("\n[T3] Aging 防止饥饿（低优先级任务等待超过阈值后升级）\n");
    cleanup_all_processes();
    aging_low_run_count = 0;
    aging_test_hog_should_stop = 0;
    aging_test_low1_initial_priority = -1;
    aging_test_low1_final_priority = -1;
    aging_test_low2_initial_priority = -1;
    aging_test_low2_final_priority = -1;
    
    // 创建高优先级任务，持续运行
    int hog = create_process(aging_test_hog_worker);
    set_priority(hog, PRIORITY_MAX);
    
    // 创建低优先级任务
    int low1 = create_process(aging_test_low_worker);
    int low2 = create_process(aging_test_low_worker);
    set_priority(low1, PRIORITY_MIN);
    set_priority(low2, PRIORITY_MIN);
    
    // 记录初始状态
    int low1_initial_prio = get_proc_priority(low1);
    int low1_initial_level = get_proc_queue_level(low1);
    int low2_initial_prio = get_proc_priority(low2);
    int low2_initial_level = get_proc_queue_level(low2);
    
    printf("  初始状态: low1(pid=%d) priority=%d level=%d, low2(pid=%d) priority=%d level=%d\n",
           low1, low1_initial_prio, low1_initial_level,
           low2, low2_initial_prio, low2_initial_level);
    
    // 让高优先级任务运行足够长时间，确保低优先级任务 wait_time >= 10
    // 每次调度都会在 age_runnable_processes() 中增加 RUNNABLE 进程的 wait_time
    // 需要运行至少 12 次调度（留一些余量），确保低优先级任务 wait_time >= 10
    printf("  运行调度器，让低优先级任务累积 wait_time...\n");
    for (int i = 0; i < 12; i++) {
        scheduler();
        // 检查低优先级任务的 wait_time（用于调试）
        spin_lock(&proc_lock);
        int low1_wait = -1, low2_wait = -1;
        for (int j = 0; j < NPROC; j++) {
            if (proc[j].pid == low1) {
                low1_wait = proc[j].wait_time;
            }
            if (proc[j].pid == low2) {
                low2_wait = proc[j].wait_time;
            }
        }
        spin_unlock(&proc_lock);
        if (i % 3 == 0) {
            printf("    调度 %d: low1 wait_time=%d, low2 wait_time=%d\n", i, low1_wait, low2_wait);
        }
    }
    
    // 检查低优先级任务的 priority 和 queue_level 是否被提升了
    int low1_after_prio = get_proc_priority(low1);
    int low1_after_level = get_proc_queue_level(low1);
    int low2_after_prio = get_proc_priority(low2);
    int low2_after_level = get_proc_queue_level(low2);
    
    printf("  Aging后状态: low1 priority=%d level=%d, low2 priority=%d level=%d\n",
           low1_after_prio, low1_after_level,
           low2_after_prio, low2_after_level);
    
    // 停止高优先级任务，让低优先级任务被调度并完成
    aging_test_hog_should_stop = 1;
    
    // 继续运行直到所有任务完成
    drive_scheduler_until_idle();
    
    struct completion_log log;
    collect_completion(&log, 3);
    print_completion("T3", &log);
    
    // 验证 aging 是否生效
    int aging_verified = 0;
    if (low1_after_prio > low1_initial_prio || low1_after_level < low1_initial_level) {
        printf("  ✓ low1 的 priority 从 %d 提升到 %d，level 从 %d 降到 %d\n",
               low1_initial_prio, low1_after_prio, low1_initial_level, low1_after_level);
        aging_verified++;
    }
    if (low2_after_prio > low2_initial_prio || low2_after_level < low2_initial_level) {
        printf("  ✓ low2 的 priority 从 %d 提升到 %d，level 从 %d 降到 %d\n",
               low2_initial_prio, low2_after_prio, low2_initial_level, low2_after_level);
        aging_verified++;
    }
    
    if (aging_verified >= 1 && aging_low_run_count == 2) {
        printf("  [PASS] Aging 防止饥饿机制生效：低优先级任务等待后成功升级\n");
    } else {
        printf("  [FAIL] Aging 未生效：aging_verified=%d, low_run_count=%d\n",
               aging_verified, aging_low_run_count);
    }
    
    cleanup_all_processes();
}
```

**输出结果**

![image-20251201110051039](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201110051039.png)

（1）初始状态设置：

进程6：高优先级任务（priority=10）

进程7、8：低优先级任务（priority=2, level=2）

（2）Aging触发过程：

等待时间累积：进程7、8在RUNNABLE状态持续等待

阈值到达：当`wait_time=10`时触发Aging机制

自动升级：

![image-20251201110308220](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251201110308220.png)

（3）升级后的调度变化：

升级前：进程6连续获得49个时间片（ticks=0→50），完全垄断CPU

升级后：进程7、8优先级提升至5，与进程6同级

获得调度：进程7、8在升级后立即获得执行机会并完成退出

### 3. 实验体会

通过本次实验，深入理解了：

（1）操作系统调度器的核心原理和实现细节

（2）多级反馈队列算法的工作机制和优势

（3）并发编程中的同步和互斥问题

（4）系统性能分析和优化方法

### 4. 改进建议
**（1）性能优化**：使用更高效的数据结构（如堆）管理就绪队列

**（2）多核支持**：实现每CPU调度队列，减少锁竞争

**（3）实时扩展**：支持硬实时任务的调度保障

**（4）能耗考虑**：集成功耗感知的调度策略