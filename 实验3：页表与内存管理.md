# 实验3：页表与内存管理

### 任务一、深入理解Sv39页表机制

##### 1、分析39位虚拟地址的分解

**分析每个VPN段的作用：**

**VPN[2] (9 bits)**: 用于在第一级页表（页目录）中索引。

**VPN[1] (9 bits)**: 用于在第二级页表（页中间目录）中索引。

**VPN[0] (9 bits)**: 用于在第三级页表（页表）中索引。

**offset (12 bits)**: 在4KB物理页内的字节偏移量。

**为什么是9位而不是其他位数？**

​	每个页表的大小是一个内存页（4KB = 2^12 bytes）。每个页表项（PTE）占8字节（64位）。因此，一页可以容纳 2^12 / 2^3 = 2^9 = 512 个页表项。所以，每一级索引需要9位（0~511）来定位512个可能的页表项。

##### 2、理解页表项（PTE）格式

每个页表项占8个字节，64位

![image-20251013145043125](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251013145043125.png)

```c
// 来自 riscv.h
typedef uint64 pte_t; // PTE 就是一个 64 位无符号整数
#define PTE_V (1L << 0) // valid
#define PTE_R (1L << 1) // readable
#define PTE_W (1L << 2) // writable
#define PTE_X (1L << 3) // executable
#define PTE_U (1L << 4) // user can access
```

---

**1. V位：有效性标志**

- **定义**: `#define PTE_V (1L << 0)`
- **作用**: 这是 PTE 的**总开关**。
- **行为**:
  - **V=1**: 该 PTE 是有效的。MMU 会继续检查其权限位并完成地址转换。
  - **V=0**: 该 PTE 是无效的。任何访问该页的尝试都会触发一个**页故障异常**，从而陷入内核，由操作系统的页故障处理程序决定如何处理。

- **软件应用场景**：
  - **惰性分配**: 在 `sbrk()` 系统调用扩大堆空间时，可以只标记页表为 `V=0`。当程序首次访问该页面触发页故障时，再实际分配物理页并设置 `V=1`。这提升了性能。
  - **Swap换出**: 当物理内存不足时，可以将一个页的内容换出到磁盘，然后将其 PTE 的 V 位清 0。当再次访问时，页故障处理程序会将其从磁盘换回。
  - **访问权限控制**: 例如，在 `exec` 加载程序时，为代码段设置 `PTE_V | PTE_R | PTE_X`，为数据段设置 `PTE_V | PTE_R | PTE_W`。

---

**2. R/W/X位：读/写/执行权限**

这三个位共同定义了页面的**访问权限**，是内存保护的核心。

- **定义**:
  - `#define PTE_R (1L << 1)` // **R**eadable
  - `#define PTE_W (1L << 2)` // **W**ritable
  - `#define PTE_X (1L << 3)` // E**x**ecutable

- **权限组合与含义**:

| R    | W    | X    | 含义                                           | 典型应用                     |
| ---- | ---- | ---- | ---------------------------------------------- | ---------------------------- |
| 1    | 0    | 0    | 只读。                                         | 只读数据段 (.rodata)         |
| 1    | 0    | 1    | 只读 & 可执行。                                | 代码段 (.text)               |
| 1    | 1    | 0    | 可读 & 可写。                                  | 数据段、堆、栈 (.data, .bss) |
| 0    | 0    | 1    | 仅可执行。                                     | 特殊架构需求                 |
| 0    | 0    | 0    | **非叶子 PTE**，指向下一级页表，而非最终页面。 | 页目录项                     |

⚠️ **关键安全规则与深入理解**

1.  **W^X (Write XOR Execute)**:
    - 这是一条至关重要的**安全原则**。它意味着一个内存页面不能同时是**可写**和**可执行**的。
    - **目的**: 防止代码注入攻击。攻击者无法将恶意代码写入一个页面然后直接执行它。
    - xv6 在设置 PTE 时，绝不会同时设置 `PTE_W` 和 `PTE_X`。

2.  **W位隐含R位**:
    - 在实践中，如果一个页面是**可写的，那么它必须是可读的**。你几乎不会遇到 `W=1` 而 `R=0` 的情况，因为这没有意义（无法读取如何写入？）。硬件和操作系统通常都遵循这个约定。

3.  **非叶子PTE的权限**:
    - 对于中间级的页表项（指向下一级页表，而非最终数据页），其 `R/W/X` 位**必须为 0**。如果被错误设置，硬件可能会将其误认为是叶子 PTE，导致不可预知的行为。

---

**3. U位：用户态访问权限**

- **定义**: `#define PTE_U (1L << 4)`
- **作用**: 控制该页面是否可以在**用户模式**下被访问。
- **行为**:
  - **U=1**: 用户模式（U-mode）和监管者模式（S-mode）都可以访问该页。
  - **U=0**: **只有监管者模式**可以访问该页。用户模式访问会触发页故障。

- **软件应用场景**：
  - **内核空间保护**: 所有内核的代码和数据页都被设置为 `U=0`。这样，当进程在用户态运行时，无法访问或修改内核内存，这是操作系统安全性的基石。
  - **用户空间定义**: 用户进程的代码、数据、堆栈等页面的 PTE 会被设置为 `U=1`。

一个典型的用户进程页表，会通过 `U` 位清晰地划分出用户空间和内核空间：
- **用户地址区间**：PTE 具有 `PTE_U`。
- **内核地址区间**（如 trampoline page, kernel stack）：PTE 没有 `PTE_U`，但在用户页表中也存在，以便在陷入内核时能顺利访问。

---

**4. 物理页号（PPN）的提取方式**

PTE 的主要作用之一是存储**物理页号**。在 Sv39 中，一个 PTE 的 [53:10] 位用于存储 PPN。

**a) PTE 到物理地址的转换**

代码中提供了关键的转换宏：

```c
// 来自 riscv.h
#define PTE2PA(pte) (((pte) >> 10) << 12)
```

**转换过程分解**：
1.  `(pte) >> 10`: 将 PTE 右移 10 位。这会将 PPN 字段（原本在 [53:10]）移动到寄存器的 [43:0] 低位，同时将低 10 位的标志位（V, R, W, X, U 等）移出。
2.  `<< 12`: 将结果左移 12 位。因为物理地址是 **4KB 对齐**的，其低 12 位永远是 0。左移 12 位就是将物理页号转换为该物理页的**基地址**。

**举个例子**：
假设一个 PTE 的值是 `0x000000008000A1`。
- 这是一个十六进制数，我们关心它的二进制位。
- 假设它的 `PPN` 部分是 `0x8000`（即二进制 `... 1000 0000 0000 0000`），且 `V=1`。
- `PTE2PA(0x8000A1)` 的计算：
  1.  `0x8000A1 >> 10 = 0x2000` (右移10位，相当于除以1024，取出了PPN)
  2.  `0x2000 << 12 = 0x2000000` (左移12位，相当于乘以4096，得到了页基地址)
- 最终得到的物理页基地址就是 `0x2000000`。

**b) 物理地址到 PTE 的转换**

逆向操作由 `PA2PTE` 宏完成：

```c
#define PA2PTE(pa) ((((uint64)pa) >> 12) << 10)
```

**转换过程分解**：
1.  `(pa) >> 12`: 将物理地址右移 12 位，得到物理页号（PPN）。
2.  `<< 10`: 将 PPN 左移 10 位，放到 PTE 的 [53:10] 位置，为添加标志位（如 `PTE_V`）腾出空间。

**c) 提取 PTE 的标志位**

有时需要单独操作标志位，而不改变 PPN：

```c
#define PTE_FLAGS(pte) ((pte) & 0x3FF)
```
- `0x3FF` 是二进制的 `11_1111_1111`（低10位全1）。

- 这个宏通过**按位与**操作，提取出 PTE 的低 10 位，即所有标志位。

  

##### 3、深入思考

 **（1）为什么选择三级页表而不是二级或四级？**

**1. 地址空间与寻址效率的平衡**

虚拟地址结构分析

Sv39 使用 **39 位虚拟地址**，页大小 **4KB**（12位偏移）：
- **可用地址空间**：2^39 = 512 GB
- **页内偏移**：12 位
- **剩余用于页表索引**：39 - 12 = 27 位

不同级数下的页表结构对比：

| 级数     | 每级索引位数        | 每级页表项数 | 总虚拟地址空间 |
| -------- | ------------------- | ------------ | -------------- |
| 二级     | 13.5 位 ❌           | 不现实       | -              |
| **三级** | **9 × 3 = 27 位** ✅ | **512 项** ✅ | **512 GB** ✅   |
| 四级     | 6.75 位 ❌           | 不现实       | -              |

**关键洞察**：9 位索引是最优解，因为：

- 每个页表正好占据 **1页内存**（4KB ÷ 8字节/项 = 512 项）
- 三级 × 9位 = 27位，完美利用所有可用寻址位

---

**2. 内存开销分析**

单个进程的页表内存占用

假设一个进程使用 1GB 内存（2^18 个页面）：

- **三级页表**：
  - 第1级：1 个页目录
  - 第2级：1 个中间目录（覆盖 1GB 范围）
  - 第3级：256 个页表（2^18 ÷ 2^9 = 512）
  - **总页数**：1 + 1 + 256 = 258 页 ≈ **1MB**

- **四级页表**：
  - 多出一级页表，内存开销增加 25-50%
  - TLB 未命中时多一次内存访问

- **二级页表**：
  - 每级需要 13.5 位，实际上需要 14 位（16384 项）
  - 每个页表需要：16384 × 8字节 = 128KB ≠ 整页
  - 造成**内部碎片**，内存利用率低

---

**3. 性能权衡：时间 vs 空间**

TLB 未命中成本

当 TLB 不命中时，需要遍历页表：

| 级数     | 内存访问次数 | 相对性能 |
| -------- | ------------ | -------- |
| 二级     | 3 次         | 基准     |
| **三级** | **4 次**     | **-25%** |
| 四级     | 5 次         | **-40%** |

**三级页表的优势**：
- 比四级页表减少 1 次内存访问（20% 性能提升）
- 比二级页表只多 1 次访问，但获得更好的空间效率5. 具体案例分析

---

**总结：三级页表的优势**

| 维度           | 二级页表       | **三级页表**     | 四级页表  |
| -------------- | -------------- | ---------------- | --------- |
| **内存效率**   | 差（内部碎片） | **优（无碎片）** | 中        |
| **访问性能**   | **优（3次）**  | 良（4次）        | 差（5次） |
| **实现复杂度** | 简单           | **适中**         | 复杂      |
| **地址空间**   | 受限           | **512GB ✅**      | 过度      |
| **教学价值**   | 过于简单       | **恰到好处**     | 过于复杂  |



### 任务二、分析xv6的物理内存分配器

---

##### 1. 核心数据结构分析

**a) 空闲页控制块 `struct run`**

```c
struct run {
  struct run *next;
};
```

**巧妙之处**：

1. **零元数据开销**：在**空闲状态**时，物理页本身存储 `next` 指针。分配后，整个页交给请求者，无任何额外开销。
2. **自我描述性**：每个空闲页都包含了找到下一个空闲页的必要信息。
3. **极简设计**：只有一个指针，最小化管理开销。

**为什么不需要额外的元数据存储？**
- **空闲时**：使用页面自身存储链表指针（前8字节作为 `next` 指针）
- **分配后**：用户获得整个4096字节的页，不需要知道分配器信息
- **元数据生命周期**：元数据只在页面空闲时存在，分配后即消失
- 这种设计称为 **"嵌入元数据"** 或 **"零开销分配"**

**b) 全局内存管理结构 `kmem`**

```c
struct {
  struct spinlock lock;
  struct run *freelist;
} kmem;
```
- `lock`：保护空闲链表的并发访问
- `freelist`：指向第一个空闲页的链表头

---

##### 2. kinit()初始化过程分析

**`kinit()` 函数**

```c
void kinit() {
  initlock(&kmem.lock, "kmem");
  freerange(end, (void*)PHYSTOP);
}
```

**如何确定可分配的内存范围？**

- `end`：来自链接脚本（`kernel.ld`），指向内核代码+数据结束后的第一个地址
- `PHYSTOP`：物理内存顶端（定义在 `memlayout.h`，通常是 0x88000000）
- **范围**：从内核结束到物理内存顶部的所有内存
- **设计思想**：内核占用低地址内存，剩余内存全部用于动态分配

**`freerange()` 函数**

```c
void freerange(void *pa_start, void *pa_end) {
  char *p;
  p = (char*)PGROUNDUP((uint64)pa_start);
  for(; p + PGSIZE <= (char*)pa_end; p += PGSIZE)
    kfree(p);
}
```

**空闲页链表的构建过程**：
1. **页对齐**：`PGROUNDUP` 确保从页边界开始
2. **逐页释放**：循环调用 `kfree()` 将每个页加入空闲链表
3. **链表顺序**：后释放的页在链表头部（LIFO顺序），形成栈式结构

**为什么要按页对齐？**
- **硬件要求**：页表映射要求物理页按 4KB 对齐
- **性能优化**：对齐的内存访问更高效，避免多次内存访问
- **简化管理**：统一大小的块易于管理，避免外部碎片
- **地址操作**：对齐地址便于使用位操作进行地址计算

---

##### 3. 理解 kalloc() 和 kfree() 的实现

**`kfree()` 函数详细分析**

```c
void kfree(void *pa) {
  struct run *r;

  // 1. 安全性检查
  if(((uint64)pa % PGSIZE) != 0 || (char*)pa < end || (uint64)pa >= PHYSTOP)
    panic("kfree");

  // 2. 填充垃圾数据（调试用）
  memset(pa, 1, PGSIZE);

  // 3. 加入空闲链表
  r = (struct run*)pa;
  acquire(&kmem.lock);
  r->next = kmem.freelist;  // 头插法
  kmem.freelist = r;
  release(&kmem.lock);
}
```

**如何防止 double-free？**
- **无直接防护**：xv6 的这个简单版本**没有**显式防止 double-free 的机制
- **间接防护**：
  - 垃圾数据填充：如果重复释放，可能因链表结构破坏而崩溃
  - 内核编程规范：依赖开发者正确使用API
  - 在复杂系统中需要添加已分配页的位图或哈希表来检测

**`kalloc()` 函数详细分析**  

```c
void *kalloc(void) {
  struct run *r;

  acquire(&kmem.lock);
  r = kmem.freelist;
  if(r)
    kmem.freelist = r->next;  // 从头部移除
  release(&kmem.lock);

  if(r)
    memset((char*)r, 5, PGSIZE); // 填充垃圾数据
  return (void*)r;
}
```

**分配算法时间复杂度**：
- **O(1)**：无论有多少空闲页，分配操作都是常数时间
- 只需要修改链表头指针，无需遍历

---

##### 4. 设计优缺点分析

**✅ 优点**

1. **极其简单**：代码量少，易于理解和调试
2. **零内存开销**：空闲时使用页面自身存储元数据，分配后无开销
3. **高性能**：分配和释放都是 O(1) 操作
4. **缓存友好**：LIFO 顺序可能提供较好的缓存局部性
5. **无外部碎片**：固定大小分配完全避免外部碎片

**❌ 缺点**

1. **功能有限**：
   - 只能分配整页，不支持小块内存
   - 无高级功能（统计、调试、内存检测）
2. **安全性不足**：
   - 无 double-free 检测
   - 无 use-after-free 防护
3. **性能问题**：
   - 单链表锁可能成为多核瓶颈（锁竞争）
   - 分配顺序可能导致内存碎片
4. **扩展性差**：难以支持不同大小的内存分配

---

##### 5. 设计思考与扩展

**实现内存统计功能**

```c
// 扩展 kmem 结构
struct {
  struct spinlock lock;
  struct run *freelist;
  uint64 total_pages;      // 总物理页数
  uint64 allocated_pages;  // 当前已分配页数
  uint64 alloc_calls;      // kalloc 调用次数
  uint64 free_calls;       // kfree 调用次数
} kmem;

// 在 kalloc() 中增加统计
kmem.allocated_pages++;
kmem.alloc_calls++;

// 在 kfree() 中增加统计  
kmem.allocated_pages--;
kmem.free_calls++;

// 添加查询接口
uint64 get_memory_stats() {
  return kmem.allocated_pages;
}
```

**检测内存泄漏**

```c
// 方法1：定期检查分配/释放平衡
void check_memory_leak() {
  if (kmem.alloc_calls - kmem.free_calls != kmem.allocated_pages) {
    panic("memory accounting inconsistent");
  }
  if (kmem.allocated_pages > LEAK_THRESHOLD) {
    printf("Warning: possible memory leak, %ld pages allocated\n", 
           kmem.allocated_pages);
  }
}

// 方法2：分配追踪（更复杂但更准确）
struct allocation_record {
  void *page;
  const char *caller;
  uint64 timestamp;
  uint64 size;
};
```

**更高效的分配算法**

1. **伙伴系统（Buddy System）**：
   
   ```c
   // 支持2的幂次方大小分配
   // 减少外部碎片，合并相邻空闲块
   // 用于Linux内核页面分配
   ```
   
2. **SLAB分配器**：
   ```c
   // 针对小对象优化，缓存热点对象
   // 更好的缓存利用率，减少初始化开销
   // 用于内核数据结构分配
   ```

3. **每CPU空闲链表（Per-CPU Freelist）**：
   ```c
   // 减少锁竞争，每个CPU维护自己的空闲链表
   // 适合多核系统，提高并发性能
   struct kmem_cache {
       struct spinlock lock;
       struct run *freelist[NCPU];
   };
   ```

4. **分级分配器（Segregated Lists）**：
   ```c
   // 对不同大小的请求使用不同链表
   // 结合大小分类和缓存策略
   struct kmem_cache {
       struct run *small_list;   // 小对象
       struct run *medium_list;  // 中等对象  
       struct run *large_list;   // 大对象
   };
   ```



### 任务三、设计你的物理内存管理器

##### 1. 内存布局方案

```c
// 在 memlayout.h 中定义内存布局
#define KERNEL_BASE   0x80000000  // 内核起始地址
#define PHYSTOP       0x88000000  // 物理内存顶端

// 内存区域划分
struct mem_zone {
    uint64 start;          // 区域起始地址
    uint64 size;           // 区域大小（字节）
    uint64 page_count;     // 总页数
    uint64 free_pages;     // 空闲页数
    uint8 *bitmap;         // 页分配位图
    struct spinlock lock;  // 区域锁
};

#define MAX_ZONES 4
struct mem_zone zones[MAX_ZONES];
int zone_count = 0;
```

**内存布局**：
```
0x80000000 - 0x80200000: 内核代码和数据（固定）
0x80200000 - 0x88000000: 可分配内存（动态管理）
```

---

##### 2. 核心数据结构设计

```c
// 物理内存管理器全局结构
struct physical_mm {
    struct spinlock lock;          // 全局锁
    struct mem_zone *zones;        // 内存区域数组
    int zone_count;                // 区域数量
    uint64 total_pages;            // 总页数
    uint64 free_pages;             // 空闲页数
    uint64 alloc_count;            // 分配计数
    uint64 free_count;             // 释放计数
    
    // 统计信息
    uint64 alloc_failures;         // 分配失败次数
    uint64 double_free_attempts;   // 重复释放尝试
};

struct physical_mm pmm;
```

**位图设计原理**：
- 每个页对应位图中的 1 位
- `bitmap[i] = 1` 表示第 i 页已分配
- `bitmap[i] = 0` 表示第 i 页空闲
- 位图本身存放在可分配内存的起始部分

---

##### 3. 接口实现

**a) 初始化函数**

```c
void pmm_init(void) {
    initlock(&pmm.lock, "pmm");
    
    // 计算可用内存范围
    uint64 alloc_start = PGROUNDUP((uint64)end);  // 内核结束后的第一个页
    uint64 alloc_end = PHYSTOP;
    uint64 alloc_size = alloc_end - alloc_start;
    
    printf("pmm: initializing, available memory: %d MB\n", 
           alloc_size / (1024 * 1024));
    
    // 创建单个内存区域（可扩展为多区域）
    struct mem_zone *zone = &zones[0];
    zone->start = alloc_start;
    zone->size = alloc_size;
    zone->page_count = alloc_size / PGSIZE;
    zone->free_pages = zone->page_count;
    
    // 计算位图大小（每个页1位）
    uint64 bitmap_size = (zone->page_count + 7) / 8;  // 向上取整到字节
    zone->bitmap = (uint8*)alloc_start;
    
    // 初始化位图（全部设为0，表示空闲）
    memset(zone->bitmap, 0, bitmap_size);
    
    // 位图本身占用的页标记为已分配
    uint64 bitmap_pages = (bitmap_size + PGSIZE - 1) / PGSIZE;
    for (int i = 0; i < bitmap_pages; i++) {
        zone->bitmap[i / 8] |= (1 << (i % 8));
        zone->free_pages--;
    }
    
    initlock(&zone->lock, "zone0");
    pmm.zones = zones;
    pmm.zone_count = 1;
    pmm.total_pages = zone->page_count;
    pmm.free_pages = zone->free_pages;
    
    printf("pmm: initialized, %d free pages out of %d total\n",
           pmm.free_pages, pmm.total_pages);
}
```

**b) 单页分配**

```c
void* alloc_page(void) {
    acquire(&pmm.lock);
    
    if (pmm.free_pages == 0) {
        pmm.alloc_failures++;
        release(&pmm.lock);
        return 0;  // 内存不足
    }
    
    // 遍历所有区域寻找空闲页
    for (int z = 0; z < pmm.zone_count; z++) {
        struct mem_zone *zone = &pmm.zones[z];
        acquire(&zone->lock);
        
        for (uint64 i = 0; i < zone->page_count; i++) {
            if ((zone->bitmap[i / 8] & (1 << (i % 8))) == 0) {
                // 找到空闲页，标记为已分配
                zone->bitmap[i / 8] |= (1 << (i % 8));
                zone->free_pages--;
                pmm.free_pages--;
                pmm.alloc_count++;
                
                void *page = (void*)(zone->start + i * PGSIZE);
                
                release(&zone->lock);
                release(&pmm.lock);
                
                // 填充垃圾数据用于调试
                memset(page, 0xAA, PGSIZE);
                return page;
            }
        }
        release(&zone->lock);
    }
    
    pmm.alloc_failures++;
    release(&pmm.lock);
    return 0;
}
```

**c) 单页释放**

```c
void free_page(void* page) {
    if (page == 0) {
        printf("pmm: attempt to free null pointer\n");
        return;
    }
    
    // 检查页对齐
    if ((uint64)page % PGSIZE != 0) {
        panic("pmm: free_page on unaligned address");
    }
    
    acquire(&pmm.lock);
    
    // 查找页面所属的区域
    for (int z = 0; z < pmm.zone_count; z++) {
        struct mem_zone *zone = &pmm.zones[z];
        if ((uint64)page >= zone->start && 
            (uint64)page < zone->start + zone->size) {
            
            acquire(&zone->lock);
            uint64 page_index = ((uint64)page - zone->start) / PGSIZE;
            
            // 检查是否已经释放（防止double-free）
            if ((zone->bitmap[page_index / 8] & (1 << (page_index % 8))) == 0) {
                pmm.double_free_attempts++;
                printf("pmm: double-free detected at %p\n", page);
                release(&zone->lock);
                release(&pmm.lock);
                return;
            }
            
            // 标记为空闲
            zone->bitmap[page_index / 8] &= ~(1 << (page_index % 8));
            zone->free_pages++;
            pmm.free_pages++;
            pmm.free_count++;
            
            release(&zone->lock);
            release(&pmm.lock);
            
            // 填充垃圾数据用于检测use-after-free
            memset(page, 0xBB, PGSIZE);
            return;
        }
    }
    
    // 页面不在任何已知区域
    panic("pmm: free_page on invalid address %p", page);
}
```

**d) 连续多页分配（可选）**

```c
void* alloc_pages(int n) {
    if (n <= 0) return 0;
    if (n == 1) return alloc_page();
    
    acquire(&pmm.lock);
    
    if (pmm.free_pages < n) {
        pmm.alloc_failures++;
        release(&pmm.lock);
        return 0;
    }
    
    // 在所有区域中寻找连续的n个空闲页
    for (int z = 0; z < pmm.zone_count; z++) {
        struct mem_zone *zone = &pmm.zones[z];
        acquire(&zone->lock);
        
        for (uint64 start = 0; start <= zone->page_count - n; start++) {
            int found = 1;
            
            // 检查连续的n页是否都空闲
            for (int i = 0; i < n; i++) {
                if ((zone->bitmap[(start + i) / 8] & (1 << ((start + i) % 8))) != 0) {
                    found = 0;
                    start += i;  // 跳过已检查的部分
                    break;
                }
            }
            
            if (found) {
                // 标记所有页为已分配
                for (int i = 0; i < n; i++) {
                    zone->bitmap[(start + i) / 8] |= (1 << ((start + i) % 8));
                }
                
                zone->free_pages -= n;
                pmm.free_pages -= n;
                pmm.alloc_count += n;
                
                void *base_page = (void*)(zone->start + start * PGSIZE);
                
                release(&zone->lock);
                release(&pmm.lock);
                
                // 初始化所有页面
                for (int i = 0; i < n; i++) {
                    memset((char*)base_page + i * PGSIZE, 0xCC, PGSIZE);
                }
                return base_page;
            }
        }
        release(&zone->lock);
    }
    
    pmm.alloc_failures++;
    release(&pmm.lock);
    return 0;
}
```

---

##### 🔧 关键设计决策解答

###### 1. 如何确定可用内存范围？
- **方案**：使用链接脚本提供的 `end` 符号作为起始，`PHYSTOP` 作为结束
- **优势**：与内核布局解耦，自动适应不同配置
- **扩展性**：支持多个不连续的内存区域

###### 2. 如何处理内存碎片？
- **外部碎片**：固定大小分配（4KB页）完全避免外部碎片
- **内部碎片**：应用程序内部管理，分配器不关心
- **连续分配**：`alloc_pages()` 使用首次适应算法寻找连续空间

###### 3. 是否需要支持不同大小的分配？
- **决策**：**否**，保持简单性
- **理由**：
  - 教学系统主要需要页级分配
  - 小块内存分配应由 SLAB 分配器处理
  - 避免复杂性，专注于核心内存管理



### 任务四、理解xv6的页表管理

---

##### 1. `walk()` 函数深度分析

**函数原型和作用**

```c
pte_t *walk(pagetable_t pagetable, uint64 va, int alloc)
```
**作用**：遍历页表，返回虚拟地址 `va` 对应的页表项指针。如果 `alloc=1`，必要时创建中间页表。

**虚拟地址索引提取**

```c
for(int level = 2; level > 0; level--) {
    pte_t *pte = &pagetable[PX(level, va)];
    // ...
}
```

**PX 宏的工作原理**：

```c
#define PXMASK          0x1FF // 9 bits
#define PXSHIFT(level)  (PGSHIFT+(9*(level)))  // 12 + 9*level
#define PX(level, va) ((((uint64) (va)) >> PXSHIFT(level)) & PXMASK)
```

**地址分解示例**（虚拟地址 0x0000000080001234）：
```
Level 2: bits 30-38: (va >> 30) & 0x1FF = (0x80001234 >> 30) & 0x1FF = 2
Level 1: bits 21-29: (va >> 21) & 0x1FF = (0x80001234 >> 21) & 0x1FF = 1  
Level 0: bits 12-20: (va >> 12) & 0x1FF = (0x80001234 >> 12) & 0x1FF = 0
```

**无效页表项处理**

```c
if(*pte & PTE_V) {
    // 有效：继续到下一级
    pagetable = (pagetable_t)PTE2PA(*pte);
} else {
    // 无效：根据alloc决定是否创建新页表
    if(!alloc || (pagetable = (pde_t*)kalloc()) == 0)
        return 0;
    memset(pagetable, 0, PGSIZE);
    *pte = PA2PTE(pagetable) | PTE_V;  // 设置新页表
}
```

**`alloc` 参数的作用**

- **alloc = 0**：只查找，不创建。用于查询映射是否存在
- **alloc = 1**：必要时创建中间页表。用于建立新映射

---

##### 2. `mappages()` 函数分析

**地址对齐处理**

```c
if((va % PGSIZE) != 0)
    panic("mappages: va not aligned");
if((size % PGSIZE) != 0)
    panic("mappages: size not aligned");
```
**要求**：虚拟地址和大小都必须是页对齐的（4KB 倍数）。

**权限位设置**

```c
*pte = PA2PTE(pa) | perm | PTE_V;
```
**权限位组合**：
- `perm`：调用者传入的权限（如 `PTE_R | PTE_W`）
- `PTE_V`：必须设置，标记页表项有效
- `PA2PTE(pa)`：将物理地址转换为 PTE 格式

**映射失败清理**

```c
if((pte = walk(pagetable, a, 1)) == 0)
    return -1;  // walk失败，整个映射操作失败
if(*pte & PTE_V)
    panic("mappages: remap");  // 重复映射，直接panic
```
**清理策略**：`mappages` 本身不处理部分失败的情况，调用者需要负责清理。

---

##### 3. 地址转换宏定义解析

**页对齐宏**

```c
#define PGROUNDUP(sz)  (((sz)+PGSIZE-1) & ~(PGSIZE-1))
#define PGROUNDDOWN(a) (((a)) & ~(PGSIZE-1))
```

**PGROUNDUP 工作原理**（假设 PGSIZE=4096=0x1000）：
```
sz = 0x1234
sz + PGSIZE-1 = 0x1234 + 0xFFF = 0x2233
~(PGSIZE-1) = ~0xFFF = 0xFFFFF000
0x2233 & 0xFFFFF000 = 0x2000  // 向上对齐到0x2000
```

**PGROUNDDOWN 工作原理**：
```
a = 0x1234
~(PGSIZE-1) = 0xFFFFF000  
0x1234 & 0xFFFFF000 = 0x1000  // 向下对齐到0x1000
```

**物理地址提取宏**

```c
#define PTE_PA(pte) (((pte) >> 10) << 12)
```

**转换过程**：
```
假设 PTE = 0x8000A1 (PPN=0x8000, flags=0xA1)
PTE >> 10 = 0x2000     // 移除低10位标志
0x2000 << 12 = 0x2000000  // 得到物理页基地址
```

---

#### 🚨 实现挑战与解决方案

**1. 避免页表遍历中的无限递归**

**问题**：如果页表指向自身，会导致无限递归。

**xv6 的防护**：
- 使用 `MAXVA` 检查虚拟地址范围
- 页表物理地址通过 `PTE2PA` 提取，确保不指向自身
- 内核页表使用恒等映射，避免循环

**2. 映射过程中的内存分配失败恢复**

**xv6 的策略**：

```c
// 在 uvmalloc 中的处理
if(mappages(pagetable, a, PGSIZE, (uint64)mem, PTE_R|PTE_U|xperm) != 0){
    kfree(mem);                    // 释放已分配的物理页
    uvmdealloc(pagetable, a, oldsz); // 回滚已建立的映射
    return 0;
}
```

**恢复步骤**：
1. 释放刚分配的物理内存
2. 取消已建立的部分映射
3. 返回错误，让调用者处理

**3. 确保页表的一致性**

**并发保护**：

- 每个进程有自己的页表，通过进程锁保护
- `walk()` 函数本身无锁，调用者需确保正确同步

**TLB 一致性**：
```c
void kvminithart() {
    sfence_vma();  // 等待之前的内存写入完成
    w_satp(MAKE_SATP(kernel_pagetable));
    sfence_vma();  // 刷新 TLB
}
```

**写时复制的一致性**（在 `uvmcopy` 中）：
```c
// 原子性操作：先复制数据，再建立映射
memmove(mem, (char*)pa, PGSIZE);          // 复制数据
if(mappages(new, i, PGSIZE, (uint64)mem, flags) != 0) {
    kfree(mem);  // 映射失败时清理
    goto err;
}
```

---

#### 🔧 关键设计模式

**1. 惰性分配模式**

```c
uint64 vmfault(pagetable_t pagetable, uint64 va, int read) {
    // 检查地址有效性
    if (va >= p->sz) return 0;
    
    // 分配物理页
    mem = (uint64) kalloc();
    if(mem == 0) return 0;
    
    // 建立映射
    if (mappages(p->pagetable, va, PGSIZE, mem, PTE_W|PTE_U|PTE_R) != 0) {
        kfree((void *)mem);
        return 0;
    }
    return mem;
}
```

**2. 错误恢复模式**

```c
int uvmcopy(pagetable_t old, pagetable_t new, uint64 sz) {
    for(i = 0; i < sz; i += PGSIZE){
        // 尝试复制每一页
        if(分配或映射失败) {
            goto err;  // 跳转到清理代码
        }
    }
    return 0;

err:
    uvmunmap(new, 0, i / PGSIZE, 1);  // 清理已建立的部分映射
    return -1;
}
```

**3. 递归释放模式**

```c
void freewalk(pagetable_t pagetable) {
    for(int i = 0; i < 512; i++){
        pte_t pte = pagetable[i];
        if((pte & PTE_V) && (pte & (PTE_R|PTE_W|PTE_X)) == 0){
            // 中间页表：递归释放
            uint64 child = PTE2PA(pte);
            freewalk((pagetable_t)child);
        } else if(pte & PTE_V){
            panic("freewalk: leaf");  // 叶子页表应该已被清除
        }
    }
    kfree((void*)pagetable);
}
```

基于对 xv6 页表管理的深入分析，我来设计一个完整的页表管理系统。这个设计在 xv6 的基础上进行了改进，提供了更好的模块化和错误处理。



### 任务五、实现你的页表管理系统

##### 头文件定义 (`pagetable.h`)
```c
#ifndef _PAGETABLE_H_
#define _PAGETABLE_H_

#include "types.h"
#include "param.h"

// 页表类型定义
typedef uint64* pagetable_t;
typedef uint64 pte_t;

// 基本操作接口
pagetable_t create_pagetable(void);
int map_page(pagetable_t pt, uint64 va, uint64 pa, int perm);
int unmap_page(pagetable_t pt, uint64 va);
void destroy_pagetable(pagetable_t pt);

// 查询接口
pte_t* walk_lookup(pagetable_t pt, uint64 va);
uint64 virt_to_phys(pagetable_t pt, uint64 va);
int is_mapped(pagetable_t pt, uint64 va);

// 调试接口
void dump_pagetable(pagetable_t pt, int level);
void print_mappings(pagetable_t pt, uint64 start, uint64 end);

// 权限位定义
#define PTE_V (1L << 0)  // Valid
#define PTE_R (1L << 1)  // Read
#define PTE_W (1L << 2)  // Write  
#define PTE_X (1L << 3)  // Execute
#define PTE_U (1L << 4)  // User

#endif
```

---

#### 🔧 实现步骤指导

**1. 地址解析实现**

```c
// pagetable.c
#include "pagetable.h"
#include "spinlock.h"
#include "kalloc.h"
#include "printf.h"

// 从虚拟地址提取页表索引
#define VPN_SHIFT(level) (12 + 9 * (level))
#define VPN_MASK(va, level) (((va) >> VPN_SHIFT(level)) & 0x1FF)

// 地址转换宏
#define PTE2PA(pte) (((pte) >> 10) << 12)
#define PA2PTE(pa) ((((uint64)(pa)) >> 12) << 10)
#define PTE_FLAGS(pte) ((pte) & 0x3FF)

// 页对齐宏
#define PGROUNDUP(sz)  (((sz) + PGSIZE - 1) & ~(PGSIZE - 1))
#define PGROUNDDOWN(a) (((a)) & ~(PGSIZE - 1))
```

**2. 页表遍历实现**

a) 查找页表项（不创建）

```c
// 查找页表项，不创建新页表
pte_t* walk_lookup(pagetable_t pt, uint64 va) {
    if (va >= MAXVA) {
        return 0;
    }
    
    pagetable_t current = pt;
    
    // 遍历前两级页表
    for (int level = 2; level > 0; level--) {
        pte_t *pte = &current[VPN_MASK(va, level)];
        if (!(*pte & PTE_V)) {
            return 0;  // 中间页表不存在
        }
        current = (pagetable_t)PTE2PA(*pte);
    }
    
    // 返回最后一级页表项
    return &current[VPN_MASK(va, 0)];
}
```

b) 查找或创建页表项

```c
// 查找页表项，必要时创建中间页表
pte_t* walk_create(pagetable_t pt, uint64 va) {
    if (va >= MAXVA) {
        return 0;
    }
    
    pagetable_t current = pt;
    
    for (int level = 2; level > 0; level--) {
        pte_t *pte = &current[VPN_MASK(va, level)];
        
        if (*pte & PTE_V) {
            // 页表存在，继续遍历
            current = (pagetable_t)PTE2PA(*pte);
        } else {
            // 分配新页表
            pagetable_t new_table = (pagetable_t)kalloc();
            if (!new_table) {
                return 0;  // 内存不足
            }
            
            // 初始化新页表
            memset(new_table, 0, PGSIZE);
            
            // 在当前级别建立指向新页表的映射
            *pte = PA2PTE(new_table) | PTE_V;
            current = new_table;
        }
    }
    
    return &current[VPN_MASK(va, 0)];
}
```

**3. 映射建立实现**

a) 创建空页表

```c
pagetable_t create_pagetable(void) {
    pagetable_t pt = (pagetable_t)kalloc();
    if (!pt) {
        return 0;
    }
    
    // 初始化页表为全零（所有PTE无效）
    memset(pt, 0, PGSIZE);
    return pt;
}
```

b) 建立页映射

```c
int map_page(pagetable_t pt, uint64 va, uint64 pa, int perm) {
    // 检查地址对齐
    if ((va % PGSIZE) != 0 || (pa % PGSIZE) != 0) {
        return -1;  // 地址未页对齐
    }
    
    // 检查权限位有效性
    if ((perm & ~(PTE_V | PTE_R | PTE_W | PTE_X | PTE_U)) != 0) {
        return -1;  // 无效权限位
    }
    
    // 查找或创建页表项
    pte_t *pte = walk_create(pt, va);
    if (!pte) {
        return -1;  // 页表分配失败
    }
    
    // 检查是否已映射
    if (*pte & PTE_V) {
        return -2;  // 重复映射
    }
    
    // 建立映射
    *pte = PA2PTE(pa) | perm | PTE_V;
    return 0;  // 成功
}
```

c) 取消页映射

```c
int unmap_page(pagetable_t pt, uint64 va) {
    if ((va % PGSIZE) != 0) {
        return -1;  // 地址未页对齐
    }
    
    pte_t *pte = walk_lookup(pt, va);
    if (!pte || !(*pte & PTE_V)) {
        return -1;  // 映射不存在
    }
    
    // 清除页表项
    *pte = 0;
    return 0;
}
```

d) 销毁整个页表

```c
// 递归释放页表（内部辅助函数）
static void free_pagetable_recursive(pagetable_t pt, int level) {
    for (int i = 0; i < 512; i++) {
        pte_t pte = pt[i];
        if (pte & PTE_V) {
            if ((pte & (PTE_R | PTE_W | PTE_X)) == 0) {
                // 中间页表：递归释放
                pagetable_t child = (pagetable_t)PTE2PA(pte);
                free_pagetable_recursive(child, level - 1);
            }
            // 叶子页表：由调用者负责释放物理页
        }
    }
    kfree((void*)pt);
}

void destroy_pagetable(pagetable_t pt) {
    if (!pt) return;
    
    // 递归释放所有中间页表
    free_pagetable_recursive(pt, 2);
}
```

---

#### 🐛 调试检查点实现

**页表内容打印**

```c
void dump_pagetable(pagetable_t pt, int level) {
    if (!pt) return;
    
    printf("=== Page Table Level %d at %p ===\n", level, pt);
    
    for (int i = 0; i < 512; i++) {
        pte_t pte = pt[i];
        if (pte & PTE_V) {
            printf("  [%03d]: pte=%p ", i, pte);
            
            // 显示权限位
            printf("Perm[");
            if (pte & PTE_R) printf("R");
            if (pte & PTE_W) printf("W"); 
            if (pte & PTE_X) printf("X");
            if (pte & PTE_U) printf("U");
            printf("] ");
            
            if ((pte & (PTE_R | PTE_W | PTE_X)) == 0) {
                // 中间页表
                pagetable_t child = (pagetable_t)PTE2PA(pte);
                printf("-> L%d %p\n", level - 1, child);
                
                // 递归打印下一级（限制深度）
                if (level > 0) {
                    dump_pagetable(child, level - 1);
                }
            } else {
                // 叶子页表
                uint64 pa = PTE2PA(pte);
                printf("-> PA %p\n", pa);
            }
        }
    }
}
```

**映射关系打印**

```c
void print_mappings(pagetable_t pt, uint64 start, uint64 end) {
    printf("=== Virtual to Physical Mappings (%p - %p) ===\n", start, end);
    
    for (uint64 va = PGROUNDDOWN(start); va < end; va += PGSIZE) {
        pte_t *pte = walk_lookup(pt, va);
        if (pte && (*pte & PTE_V)) {
            uint64 pa = PTE2PA(*pte);
            
            printf("  VA %p -> PA %p [", va, pa);
            if (*pte & PTE_R) printf("R");
            if (*pte & PTE_W) printf("W");
            if (*pte & PTE_X) printf("X"); 
            if (*pte & PTE_U) printf("U");
            printf("]\n");
        }
    }
}
```

**查询接口实现**

```c
uint64 virt_to_phys(pagetable_t pt, uint64 va) {
    pte_t *pte = walk_lookup(pt, va);
    if (!pte || !(*pte & PTE_V)) {
        return 0;  // 未映射或无效
    }
    return PTE2PA(*pte) | (va & 0xFFF);  // 物理地址 + 页内偏移
}

int is_mapped(pagetable_t pt, uint64 va) {
    pte_t *pte = walk_lookup(pt, va);
    return (pte && (*pte & PTE_V));
}
```

---

#### 🧪 测试用例

```c
// 测试函数
void test_pagetable_system(void) {
    printf("Testing pagetable system...\n");
    
    // 1. 创建页表
    pagetable_t pt = create_pagetable();
    if (!pt) {
        printf("FAIL: create_pagetable\n");
        return;
    }
    
    // 2. 建立映射
    uint64 va = 0x40000000;  // 虚拟地址
    uint64 pa = 0x80000000;  // 物理地址
    
    if (map_page(pt, va, pa, PTE_R | PTE_W | PTE_V) != 0) {
        printf("FAIL: map_page\n");
        destroy_pagetable(pt);
        return;
    }
    
    // 3. 验证映射
    uint64 translated = virt_to_phys(pt, va);
    if (translated != (pa | (va & 0xFFF))) {
        printf("FAIL: address translation\n");
        destroy_pagetable(pt);
        return;
    }
    
    // 4. 打印调试信息
    printf("Page table dump:\n");
    dump_pagetable(pt, 2);
    
    printf("Mappings:\n");
    print_mappings(pt, 0x40000000, 0x40001000);
    
    // 5. 清理
    destroy_pagetable(pt);
    printf("SUCCESS: pagetable system test passed\n");
}
```



### 任务六、启用虚拟内存

基于对 xv6 虚拟内存启用代码的深入分析，我来详细解释内核页表创建和激活的关键技术细节。

##### 🏗️ 1. 内核页表创建分析

**`kvmmake()` 函数：内核页表构建**

```c
pagetable_t kvmmake(void) {
    pagetable_t kpgtbl;
    kpgtbl = (pagetable_t) kalloc();
    memset(kpgtbl, 0, PGSIZE);
```

**初始化**：分配并清零一个页作为根页表。

**内存区域映射分析**

**a) 设备内存映射**

```c
// uart registers
kvmmap(kpgtbl, UART0, UART0, PGSIZE, PTE_R | PTE_W);

// virtio mmio disk interface  
kvmmap(kpgtbl, VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W);

// PLIC (Platform-Level Interrupt Controller)
kvmmap(kpgtbl, PLIC, PLIC, 0x4000000, PTE_R | PTE_W);
```

**设备内存权限**：`PTE_R | PTE_W`
- **原因**：设备寄存器需要读写访问，但不能执行
- **安全性**：防止恶意代码执行设备内存

**b) 内核代码段映射**

```c
// map kernel text executable and read-only.
kvmmap(kpgtbl, KERNBASE, KERNBASE, (uint64)etext-KERNBASE, PTE_R | PTE_X);
```

**内核代码权限**：`PTE_R | PTE_X`
- **可执行**：内核代码需要被执行
- **只读**：防止意外修改内核代码，提高安全性
- **范围**：从 `KERNBASE` 到 `etext`（代码段结束）

**c) 内核数据段映射**

```c
// map kernel data and the physical RAM we'll make use of.
kvmmap(kpgtbl, (uint64)etext, (uint64)etext, PHYSTOP-(uint64)etext, PTE_R | PTE_W);
```

**内核数据权限**：`PTE_R | PTE_W`
- **可读写**：内核数据需要读写访问
- **不可执行**：数据段不应被执行
- **范围**：从 `etext` 到 `PHYSTOP`（物理内存顶端）

**d) Trampoline 页面映射**

```c
// map the trampoline for trap entry/exit
kvmmap(kpgtbl, TRAMPOLINE, (uint64)trampoline, PGSIZE, PTE_R | PTE_X);
```

**Trampoline 作用**：在用户态和内核态之间安全切换的代码
- **最高地址**：映射到虚拟地址空间顶端
- **权限**：`PTE_R | PTE_X` - 可执行，用于陷阱处理

**为什么采用恒等映射？**

**恒等映射**：虚拟地址 = 物理地址（如 `KERNBASE → KERNBASE`）

```c
// 所有内核映射都是 va = pa
kvmmap(kpgtbl, KERNBASE, KERNBASE, ...);
kvmmap(kpgtbl, UART0, UART0, ...);
```

**优势**：
1. **简化启动**：在启用分页前后，内核都能用相同的地址访问内存
2. **避免切换开销**：内核不需要在物理地址和虚拟地址之间转换
3. **设备访问**：MMIO 设备寄存器在物理地址空间有固定位置
4. **调试友好**：地址值直接反映物理位置

**内存布局示例**：

```
物理地址       虚拟地址        用途
0x80000000  → 0x80000000     内核代码
0x80002000  → 0x80002000     内核数据
0x10000000  → 0x10000000     UART设备
0x0C000000  → 0x0C000000     PLIC设备
```

---

##### ⚡ 2. 页表激活分析

**`kvminithart()` 函数：激活内核页表**

```c
void kvminithart() {
    // wait for any previous writes to the page table memory to finish.
    sfence_vma();

    w_satp(MAKE_SATP(kernel_pagetable));

    // flush stale entries from the TLB.
    sfence_vma();
}
```

**SATP 寄存器格式和设置**

**SATP 寄存器结构**

```
63      60 59      44 43      0
|   MODE   |   ASID   |    PPN    |
```

**字段说明**：
- **MODE**：页表模式
  - `8` = Sv39（39位虚拟地址）
  - `9` = Sv48（48位虚拟地址）
- **ASID**：地址空间标识符（用于TLB隔离）
- **PPN**：根页表的物理页号

**MAKE_SATP 宏**

```c
#define SATP_SV39 (8L << 60)
#define MAKE_SATP(pagetable) (SATP_SV39 | (((uint64)pagetable) >> 12))
```

**计算示例**：
```c
假设 kernel_pagetable 物理地址 = 0x87F000
MAKE_SATP(0x87F000) = (8 << 60) | (0x87F000 >> 12)
                    = 0x8000000000000000 | 0x87F
                    = 0x800000000000087F
```

**sfence.vma 指令的作用**

**第一个 sfence.vma：写屏障**

```c
sfence_vma();  // 等待之前的内存写入完成
```
**作用**：确保所有对页表内存的修改已经提交到内存中，避免CPU乱序执行导致TLB加载旧数据。

**第二个 sfence.vma：TLB刷新**

```c
w_satp(MAKE_SATP(kernel_pagetable));
sfence_vma();  // 刷新TLB
```
**作用**：刷新整个TLB，清除所有旧的地址转换缓存，确保使用新的页表。

**TLB刷新必要性**：

- 修改SATP寄存器后，CPU不知道哪些TLB条目失效
- 必须显式刷新，否则可能使用陈旧的地址转换
- 确保内存访问的一致性

---

##### 🔧 3. 实现策略与关键技术

**完整的虚拟内存启用实现**

```c
// 虚拟内存管理器头文件 (vm.h)
#ifndef _VM_H_
#define _VM_H_

#include "types.h"
#include "pagetable.h"

// 内核页表（所有CPU共享）
extern pagetable_t kernel_pagetable;

// 内存区域描述
struct mem_region {
    uint64 va_start;
    uint64 pa_start; 
    uint64 size;
    int permissions;
    const char *name;
};

// 初始化函数
void vm_init(void);
void vm_enable(void);

// 区域映射函数
int map_kernel_region(uint64 va, uint64 pa, uint64 size, int perm, const char *name);
void dump_kernel_mappings(void);

#endif
```

**内核虚拟内存初始化实现**

```c
// vm.c
#include "vm.h"
#include "memlayout.h"
#include "kalloc.h"
#include "printf.h"

pagetable_t kernel_pagetable;

// 内核内存区域定义
static struct mem_region kernel_regions[] = {
    // 设备内存区域
    {UART0, UART0, PGSIZE, PTE_R | PTE_W, "UART"},
    {VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W, "VIRTIO"},
    {PLIC, PLIC, 0x400000, PTE_R | PTE_W, "PLIC"},
    
    // 内核内存区域  
    {KERNBASE, KERNBASE, (uint64)etext - KERNBASE, PTE_R | PTE_X, "Kernel Code"},
    {(uint64)etext, (uint64)etext, PHYSTOP - (uint64)etext, PTE_R | PTE_W, "Kernel Data"},
    
    // Trampoline页面
    {TRAMPOLINE, (uint64)trampoline, PGSIZE, PTE_R | PTE_X, "Trampoline"},
    
    {0, 0, 0, 0, 0}  // 结束标记
};

void vm_init(void) {
    printf("vm: initializing kernel page table\n");
    
    // 1. 创建内核页表
    kernel_pagetable = create_pagetable();
    if (!kernel_pagetable) {
        panic("vm: failed to create kernel pagetable");
    }
    
    // 2. 映射所有内核区域
    for (struct mem_region *region = kernel_regions; region->size > 0; region++) {
        if (map_region(kernel_pagetable, region->va_start, region->pa_start, 
                      region->size, region->permissions) != 0) {
            panic("vm: failed to map %s region", region->name);
        }
        printf("vm: mapped %s: VA %p -> PA %p, size %d KB, perm ",
               region->name, region->va_start, region->pa_start, 
               region->size / 1024);
        // 打印权限位
        if (region->permissions & PTE_R) printf("R");
        if (region->permissions & PTE_W) printf("W");
        if (region->permissions & PTE_X) printf("X");
        if (region->permissions & PTE_U) printf("U");
        printf("\n");
    }
    
    // 3. 映射进程内核栈（每个进程独立）
    proc_mapstacks(kernel_pagetable);
    
    printf("vm: kernel page table initialized\n");
}

void vm_enable(void) {
    printf("vm: enabling virtual memory\n");
    
    // 内存屏障：确保所有页表写入完成
    __sync_synchronize();  // 编译器屏障
    sfence_vma();          // 架构屏障
    
    // 设置SATP寄存器启用分页
    uint64 satp_value = MAKE_SATP(kernel_pagetable);
    w_satp(satp_value);
    
    // 刷新TLB，使用新的页表
    sfence_vma();
    
    printf("vm: virtual memory enabled (SATP=%p)\n", satp_value);
}

// 区域映射辅助函数
int map_region(pagetable_t pt, uint64 va, uint64 pa, uint64 size, int perm) {
    if (mappages(pt, va, size, pa, perm) != 0) {
        return -1;
    }
    return 0;
}

void dump_kernel_mappings(void) {
    printf("=== Kernel Virtual Memory Map ===\n");
    for (struct mem_region *region = kernel_regions; region->size > 0; region++) {
        printf("  %-12s: VA %p -> PA %p, size: %d KB\n",
               region->name, region->va_start, region->pa_start, 
               region->size / 1024);
    }
}
```

**进程内核栈映射**

```c
// 为每个进程映射内核栈
void proc_mapstacks(pagetable_t kpgtbl) {
    struct proc *p;
    
    for(p = proc; p < &proc[NPROC]; p++) {
        // 为每个进程分配内核栈
        char *pa = kalloc();
        if(pa == 0)
            panic("vm: kalloc failed for kernel stack");
        
        // 计算该进程内核栈的虚拟地址
        uint64 va = KSTACK((int)(p - proc));
        
        // 映射内核栈：可读写，不可执行
        kvmmap(kpgtbl, va, (uint64)pa, PGSIZE, PTE_R | PTE_W);
    }
}
```



### 测试与调试

##### 1、物理内存初始化结果

![image-20251020112421302](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251020112421302.png)

- **物理内存范围**：从 `0x80003000` 到 `0x88000000`（约128MB）
- **可用页数**：32,765个空闲页（每个页4KB，约128MB）

##### 2、虚拟内存初始化结果

![image-20251020112509551](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251020112509551.png)

内核页表创建在物理地址 `0x87fff000`

**代码段**：虚拟地址 `0x80000000-0x8000150c`（5,388字节）

**数据段**：虚拟地址 `0x8000150c-0x88000000`

**UART设备**：映射到 `0x10000000`，可读写权限

##### 3、显示系统信息

测试代码

```
// 显示系统信息
    printf("System Information:\n");
    printf("  Kernel text: 0x80000000 - %p\n", (void*)etext);
    printf("  Kernel data: %p - %p\n", (void*)etext, (void*)&end);
    printf("  Page size: %d bytes\n", PAGE_SIZE);
```

输出结果

![image-20251013201316430](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251013201316430.png)

##### 4、物理内存分配器测试

测试代码

```
/* ==================== 物理内存分配器测试 ==================== */
void test_physical_memory(void) {
    printf("\n=== Physical Memory Manager Test ===\n");
    
    // 测试基本分配和释放
    printf("1. Testing basic allocation and free...\n");
    void *page1 = alloc_page();
    void *page2 = alloc_page();
    
    printf("   Allocated pages: page1=%p, page2=%p\n", page1, page2);
    
    if (page1 == NULL || page2 == NULL) {
        printf("   ERROR: Failed to allocate pages!\n");
        return;
    }
    
    assert(page1 != NULL);
    assert(page2 != NULL);
    assert(page1 != page2);
    assert(((uint64_t)page1 & 0xFFF) == 0);  // 页对齐检查
    assert(((uint64_t)page2 & 0xFFF) == 0);
    
    printf("   ✓ Page alignment check passed\n");

    // 测试数据写入和读取
    printf("2. Testing data read/write...\n");
    *(volatile uint32_t*)page1 = 0x12345678;
    uint32_t read_value = *(volatile uint32_t*)page1;
    printf("   Write test: wrote 0x%x, read 0x%x\n", 0x12345678, read_value);
    assert(read_value == 0x12345678);
    
    printf("   ✓ Data read/write test passed\n");

    // 测试释放和重新分配
    printf("3. Testing free and reallocation...\n");
    free_page(page1);
    void *page3 = alloc_page();
    
    printf("   Reallocated page: page3=%p (original page1=%p)\n", page3, page1);
    // page3 可能等于 page1（取决于分配策略）
    
    if (page3) {
        // 测试重新分配的页面是清零的
        for (int i = 0; i < 4; i++) {
            uint64_t value = *(volatile uint64_t*)((char*)page3 + i * 64);
            if (value != 0) {
                printf("   ERROR: Page not cleared at offset %d: 0x%llx\n", i * 64, value);
            }
        }
        printf("   ✓ Page clearing after allocation verified\n");
    }

    // 清理
    free_page(page2);
    if (page3) free_page(page3);
    
    printf("✓ Physical memory test completed successfully!\n");
}
```

输出结果

![image-20251013200922012](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251013200922012.png)



##### 5、虚拟内存激活测试

测试代码

```
printf("\n=== Phase 2: Virtual Memory Setup ===\n");
    printf("Initializing kernel page table...\n");
    kvminit();
    
    printf("Enabling virtual memory...\n");
    kvminithart();
```

输出结果

![image-20251020112723314](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251020112723314.png)



##### 6、页表功能测试

测试代码

```
/* ==================== 页表功能测试 ==================== */
void test_pagetable(void) {
    printf("\n=== Page Table Functionality Test ===\n");
    
    printf("1. Creating page table...\n");
    pagetable_t pt = create_pagetable();
    if (pt == NULL) {
        printf("   ERROR: Failed to create page table!\n");
        return;
    }
    printf("   ✓ Page table created at %p\n", pt);

    // 分配物理页用于映射测试
    printf("2. Allocating physical pages for mapping...\n");
    uint64_t pa1 = (uint64_t)alloc_page();
    uint64_t pa2 = (uint64_t)alloc_page();
    uint64_t pa3 = (uint64_t)alloc_page();
    
    if (pa1 == 0 || pa2 == 0 || pa3 == 0) {
        printf("   ERROR: Failed to allocate physical pages!\n");
        return;
    }
    printf("   ✓ Physical pages allocated: %p, %p, %p\n", (void*)pa1, (void*)pa2, (void*)pa3);

    // 测试基本映射
    printf("3. Testing basic page mapping...\n");
    uint64_t va1 = 0x40000000;  // 测试虚拟地址1
    uint64_t va2 = 0x50000000;  // 测试虚拟地址2
    uint64_t va3 = 0x60000000;  // 测试虚拟地址3
    
    int result1 = map_page(pt, va1, pa1, PTE_R | PTE_W);
    int result2 = map_page(pt, va2, pa2, PTE_R | PTE_X);
    int result3 = map_page(pt, va3, pa3, PTE_R);
    
    printf("   Mapping results: %d, %d, %d\n", result1, result2, result3);
    
    if (result1 != 0 || result2 != 0 || result3 != 0) {
        printf("   ERROR: Failed to create page mappings!\n");
        return;
    }
    printf("   ✓ Page mappings created successfully\n");

    // 测试地址转换查找
    printf("4. Testing address translation...\n");
    pte_t *pte1 = walk_lookup(pt, va1);
    pte_t *pte2 = walk_lookup(pt, va2);
    pte_t *pte3 = walk_lookup(pt, va3);
    
    printf("   Lookup results: %p, %p, %p\n", pte1, pte2, pte3);
    
    if (pte1 == NULL || pte2 == NULL || pte3 == NULL) {
        printf("   ERROR: Failed to find page table entries!\n");
        return;
    }
    
    if (!(*pte1 & PTE_V) || !(*pte2 & PTE_V) || !(*pte3 & PTE_V)) {
        printf("   ERROR: Page table entries not valid!\n");
        return;
    }
    printf("   ✓ Page table entries found and valid\n");

    // 显示页表内容
    printf("5. Dumping page table contents...\n");
    dump_pagetable(pt);

    // 清理
    printf("6. Cleaning up...\n");
    free_page((void*)pa1);
    free_page((void*)pa2);
    free_page((void*)pa3);
    
    printf("✓ Page table test completed successfully!\n");
}
```

输出结果

![image-20251020112848321](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251020112848321.png)

![image-20251020153444095](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251020153444095.png)

### 扩展功能：伙伴系统

##### **1、源代码 ** **buddy.c**

```
#include "mm.h"
#include "printf.h"
#include "buddy.h"

// 伙伴系统全局实例
static struct buddy_pool buddy_system;

//将阶数转换为对应的页数：order 0 = 1页，order 1 = 2页，order 8 = 256页
static inline int order_to_pages(int order) {
    return 1 << order;
}

// 计算地址在内存池中的页索引
static inline uint64_t addr_to_page_index(uint64_t addr) {
    if (addr < buddy_system.pool_start) return (uint64_t)-1;
    return (addr - buddy_system.pool_start) / PAGE_SIZE;
}

// 计算页索引对应的地址
static inline uint64_t page_index_to_addr(uint64_t index) {
    return buddy_system.pool_start + index * PAGE_SIZE;
}

// 获取伙伴块的页索引(异或)
static inline uint64_t get_buddy_index(uint64_t index, int order) {
    return index ^ (1 << order);
}

// 检查伙伴块是否空闲且可合并
int is_buddy_free(uint64_t index, int order) {
    uint64_t buddy_index = get_buddy_index(index, order);
    
    // 检查伙伴索引是否有效
    if (buddy_index >= buddy_system.total_pages) {
        printf("Buddy: buddy index %d invalid (total_pages=%d)\n", 
               (int)buddy_index, (int)buddy_system.total_pages);
        return 0;
    }
    
    // 检查伙伴块是否在对应阶的空闲链表中
    struct list_head *pos;
    uint64_t buddy_addr = page_index_to_addr(buddy_index);
    
    printf("Buddy: checking if buddy %p (index=%d) is free in order %d\n",
           (void*)buddy_addr, (int)buddy_index, order);
    
    list_for_each(pos, &buddy_system.free_lists[order]) {
        uint64_t pos_addr = (uint64_t)pos;
        uint64_t pos_index = addr_to_page_index(pos_addr);
        
        printf("Buddy:   comparing with free block %p (index=%d)\n",
               (void*)pos_addr, (int)pos_index);
        
        if (pos_index == buddy_index) {
            printf("Buddy:   FOUND buddy in free list!\n");
            return 1;
        }
    }
    
    printf("Buddy:   buddy NOT found in free list\n");
    return 0;
}

// 完整的伙伴系统初始化
void buddy_init(void) {
    printf("Buddy: starting initialization...\n");
    
    // 初始化空闲链表
    for (int i = 0; i <= BUDDY_MAX_ORDER; i++) {
        INIT_LIST_HEAD(&buddy_system.free_lists[i]);
    }
    
    // 设置内存池范围
    extern char end[];
    extern uint64_t kernel_base;
    buddy_system.pool_start = PGROUNDUP((uint64_t)&end);
    
    // 使用明确的 PHYSTOP 定义
    #define BUDDY_PHYSTOP ((uint64_t)kernel_base + 128*1024*1024)
    buddy_system.pool_size = BUDDY_PHYSTOP - buddy_system.pool_start;
    buddy_system.total_pages = buddy_system.pool_size / PAGE_SIZE;
    buddy_system.used_pages = 0;
    
    printf("Buddy: pool [%p, %p) size=%dKB, pages=%d\n",
           (void*)buddy_system.pool_start, 
           (void*)BUDDY_PHYSTOP,
           (int)(buddy_system.pool_size / 1024),
           (int)buddy_system.total_pages);
    
    // 暂时禁用位图
    buddy_system.bitmap = NULL;
    buddy_system.bitmap_size = 0;
    
    // 第一个可用地址就是 pool_start
    uint64_t data_start = buddy_system.pool_start;
    
    // 将整个可用内存作为一个大块添加到最大阶的空闲链表
    uint64_t available_size = BUDDY_PHYSTOP - data_start;
    uint64_t available_pages = available_size / PAGE_SIZE;
    
    printf("Buddy: available memory: %d pages from %p\n", 
           (int)available_pages, (void*)data_start);
    
    // 找到适合的最大阶数
    int max_order = BUDDY_MAX_ORDER;
    while (max_order >= 0) {
        uint64_t order_pages = order_to_pages(max_order);
        if (available_pages >= order_pages) {
            break;
        }
        max_order--;
    }
    
    if (max_order < 0) {
        printf("Buddy: ERROR: not enough memory for initialization\n");
        return;
    }
    
    // 将可用内存添加到对应阶的空闲链表
    struct list_head *block = (struct list_head*)data_start;
    INIT_LIST_HEAD(block);
    list_add(block, &buddy_system.free_lists[max_order]);
    
    printf("Buddy: initialized with max_order=%d, first block at %p (%d pages)\n", 
           max_order, (void*)data_start, order_to_pages(max_order));
    printf("Buddy: initialization completed successfully\n");
}

// 修复的分配函数
void* buddy_alloc(int order) {
    if (order < BUDDY_MIN_ORDER || order > BUDDY_MAX_ORDER) {
        printf("Buddy: invalid order %d\n", order);
        return NULL;
    }
    
    printf("Buddy: trying to allocate order %d (%d pages)\n", order, order_to_pages(order));
    
    int current_order = order;
    
    // 寻找合适阶数的空闲块
    while (current_order <= BUDDY_MAX_ORDER) {
        if (!list_empty(&buddy_system.free_lists[current_order])) {
            break;
        }
        current_order++;
    }
    
    if (current_order > BUDDY_MAX_ORDER) {
        printf("Buddy: out of memory for order %d\n", order);
        return NULL;
    }
    
    printf("Buddy: found free block at order %d\n", current_order);
    
    // 从找到的链表中取出第一个块
    struct list_head *block = buddy_system.free_lists[current_order].next;
    list_del(block);
    
    uint64_t block_addr = (uint64_t)block;
    uint64_t block_index = addr_to_page_index(block_addr);
    
    printf("Buddy: got block at %p (index=%d) from order %d\n", 
           (void*)block_addr, (int)block_index, current_order);
    
    // 如果找到的块比需要的大，进行分裂
    while (current_order > order) {
        current_order--;
        
        // 计算伙伴块
        uint64_t buddy_index = get_buddy_index(block_index, current_order);
        uint64_t buddy_addr = page_index_to_addr(buddy_index);
        
        // 检查伙伴索引是否有效
        if (buddy_index >= buddy_system.total_pages) {
            printf("Buddy: WARNING: invalid buddy index %d, stopping split\n", (int)buddy_index);
            break;
        }
        
        printf("Buddy: splitting order %d -> %d\n", current_order + 1, current_order);
        printf("Buddy: block=%p (index=%d), buddy=%p (index=%d)\n", 
               (void*)block_addr, (int)block_index, (void*)buddy_addr, (int)buddy_index);
        
        // 将伙伴块添加到对应阶的空闲链表
        struct list_head *buddy = (struct list_head*)buddy_addr;
        INIT_LIST_HEAD(buddy);
        list_add(buddy, &buddy_system.free_lists[current_order]);
        
        printf("Buddy: added buddy block to free list order %d\n", current_order);
    }
    
    buddy_system.used_pages += order_to_pages(order);
    
    printf("Buddy: allocated order %d at %p (index=%d), pages=%d\n",
           order, (void*)block_addr, (int)block_index, order_to_pages(order));
    
    return (void*)block_addr;
}

// 修复的释放函数 - 改进合并逻辑
void buddy_free(void* addr, int order) {
    if (addr == NULL || order < BUDDY_MIN_ORDER || order > BUDDY_MAX_ORDER) {
        printf("Buddy: invalid free parameters: addr=%p, order=%d\n", addr, order);
        return;
    }
    
    uint64_t block_addr = (uint64_t)addr;
    uint64_t current_index = addr_to_page_index(block_addr);
    
    printf("Buddy: freeing order %d at %p (index=%d)\n", order, addr, (int)current_index);
    
    // 验证地址有效性
    if (current_index >= buddy_system.total_pages) {
        printf("Buddy: ERROR: invalid address %p (index=%d, total_pages=%d)\n", 
               addr, (int)current_index, (int)buddy_system.total_pages);
        return;
    }
    
    int current_order = order;
    uint64_t merge_index = current_index;
    uint64_t merge_addr = block_addr;
    
    // 只要当前阶数小于最大阶数，就尝试合并
    while (current_order < BUDDY_MAX_ORDER) {
        uint64_t buddy_index = get_buddy_index(merge_index, current_order);
        
        // 检查伙伴索引是否有效
        if (buddy_index >= buddy_system.total_pages) {
            printf("Buddy: buddy index %d exceeds total pages %d, stop merging\n",
                   (int)buddy_index, (int)buddy_system.total_pages);
            break;
        }
        
        printf("Buddy: checking merge at order %d, index=%d, buddy_index=%d\n",
               current_order, (int)merge_index, (int)buddy_index);
        
        // 检查伙伴块是否空闲且可合并
        if (!is_buddy_free(merge_index, current_order)) {
            printf("Buddy: buddy not free, stop merging at order %d\n", current_order);
            break;
        }
        
        uint64_t buddy_addr = page_index_to_addr(buddy_index);
        
        printf("Buddy: merging at order %d: block=%p, buddy=%p\n",
               current_order, (void*)merge_addr, (void*)buddy_addr);
        
        // 从空闲链表中移除伙伴块
        struct list_head *buddy = (struct list_head*)buddy_addr;
        list_del(buddy);
        
        // 计算合并后的块地址（取两个块中较小的地址）
        if (buddy_index < merge_index) {
            merge_index = buddy_index;
            merge_addr = buddy_addr;
        }
        
        current_order++;
        
        printf("Buddy: merged order %d -> %d at %p (index=%d)\n",
               current_order - 1, current_order, (void*)merge_addr, (int)merge_index);
    }
    
    // 将合并后的块添加到对应阶的空闲链表
    struct list_head *block = (struct list_head*)merge_addr;
    INIT_LIST_HEAD(block);
    list_add(block, &buddy_system.free_lists[current_order]);
    
    buddy_system.used_pages -= order_to_pages(order);
    
    printf("Buddy: freed order %d, final order=%d at %p\n", 
           order, current_order, (void*)merge_addr);
}

// 简化的伙伴系统状态转储
void buddy_dump(void) {
    printf("=== Buddy System Status ===\n");
    printf("Pool: [%p, %p) total_pages=%d, used_pages=%d\n",
           (void*)buddy_system.pool_start, 
           (void*)(buddy_system.pool_start + buddy_system.pool_size),
           (int)buddy_system.total_pages, (int)buddy_system.used_pages);
    
    for (int i = 0; i <= BUDDY_MAX_ORDER; i++) {
        int count = 0;
        struct list_head *pos;
        list_for_each(pos, &buddy_system.free_lists[i]) {
            count++;
        }
        printf("Order %d (%d pages): %d free blocks\n", 
               i, order_to_pages(i), count);
    }
    printf("===========================\n");
}

// 获取总页数
uint64_t buddy_get_total_pages(void) {
    return buddy_system.total_pages;
}

// 获取已使用页数
uint64_t buddy_get_used_pages(void) {
    return buddy_system.used_pages;
}
```

##### 2、原理图

![image-20251025214355740](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025214355740.png)

##### **3、测试代码**

```
void test_buddy_system(void) {
    printf("=== Buddy System Comprehensive Test ===\n");
    
    // 初始化伙伴系统
    printf("1. Initializing buddy system...\n");
    buddy_init();
    buddy_dump();
    
    // 测试不同阶数的分配
    printf("2. Testing allocations of different orders...\n");
    void *blocks[BUDDY_MAX_ORDER + 1] = {0};
    
    // 分配一些小阶数的块
    for (int order = 0; order <= 2; order++) {
        printf("   Allocating order %d...\n", order);
        blocks[order] = buddy_alloc(order);
        if (blocks[order]) {
            printf("   ✓ Allocated order %d (%d pages) at %p\n", 
                   order, 1 << order, blocks[order]);
        } else {
            printf("   ✗ Failed to allocate order %d\n", order);
        }
        buddy_dump();
    }
    
    // 测试释放和合并
    printf("3. Testing free and merge...\n");
    for (int order = 0; order <= 2; order++) {
        if (blocks[order]) {
            printf("   Freeing order %d at %p...\n", order, blocks[order]);
            buddy_free(blocks[order], order);
            printf("   ✓ Freed order %d\n", order);
            blocks[order] = NULL;
            buddy_dump();
        }
    }
    
    // 测试连续分配和释放
    printf("4. Testing continuous allocation pattern...\n");
    void *test_blocks[4];
    for (int i = 0; i < 4; i++) {
        test_blocks[i] = buddy_alloc(0);  // 分配4个单页
        if (test_blocks[i]) {
            printf("   ✓ Allocated single page %d at %p\n", i, test_blocks[i]);
        } else {
            printf("   ✗ Failed to allocate single page %d\n", i);
        }
    }
    buddy_dump();
    
    // 释放一些块创建碎片
    printf("5. Creating fragmentation...\n");
    buddy_free(test_blocks[1], 0);
    buddy_free(test_blocks[3], 0);
    printf("   Freed pages 1 and 3 to create fragmentation\n");
    buddy_dump();
    
    // 分配一个2页的块，应该能够合并碎片
    printf("6. Testing merge by allocating 2-page block...\n");
    void *two_page_block = buddy_alloc(1);
    if (two_page_block) {
        printf("   ✓ Allocated 2-page block at %p (demonstrates merge)\n", two_page_block);
        buddy_free(two_page_block, 1);
        printf("   ✓ Freed 2-page block\n");
    } else {
        printf("   ✗ Failed to allocate 2-page block\n");
    }
    buddy_dump();
    
    // 清理剩余块
    buddy_free(test_blocks[0], 0);
    buddy_dump();
    buddy_free(test_blocks[2], 0);
    buddy_dump();
    
    printf("✓ Buddy system test completed successfully!\n\n");
}
```

##### **4、流程图**

![image-20251025214840151](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025214840151.png)



![image-20251025214858503](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025214858503.png)



##### **5、输出结果**

![image-20251025215223017](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215223017.png)

![image-20251025215240351](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215240351.png)

![image-20251025215309624](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215309624.png)

![image-20251025215336463](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215336463.png)

![image-20251025215427382](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215427382.png)

![image-20251025215456264](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215456264.png)

![image-20251025215517812](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215517812.png)

![image-20251025215541087](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215541087.png)

![image-20251025215557058](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215557058.png)

![image-20251025215627096](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215627096.png)

![image-20251025215644304](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20251025215644304.png)

