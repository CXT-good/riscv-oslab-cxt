# 实验1：RISC-V引导与裸机启动实验报告

## 一、系统设计部分

### 1、架构设计说明

​	本实验设计了一个基于RISC-V架构的最小操作系统启动流程，运行在QEMU的`virt`虚拟平台上。系统采用单核设计，简化了多核同步和调度机制，专注于实现最基础的启动流程和串口输出功能。

​	系统启动流程如下：
1. CPU从`0x80000000`地址开始执行汇编入口代码`_entry`
2. 设置栈指针，为C代码运行准备栈空间
3. 清零BSS段，确保未初始化变量具有确定值
4. 跳转到C语言的`main`函数
5. 初始化UART串口设备
6. 通过串口输出"Hello OS"等欢迎信息
7. 进入无限循环，保持系统运行

### 2、关键数据结构

​	由于是最小系统，数据结构极为简化：

1. **栈空间**：在链接脚本中定义了一个4KB的固定栈空间
2. **BSS段边界符号**：通过链接脚本定义`_bss_start`和`_bss_end`，用于清零操作
3. **UART寄存器映射**：使用内存映射IO方式访问UART设备寄存器

### 3、与xv6对比分析

| 特性       | xv6 (多核)                                | 本实现 (单核)           |
| ---------- | ----------------------------------------- | ----------------------- |
| 启动流程   | 复杂的多阶段启动，CPU0初始化后唤醒其他CPU | 简单的线性启动流程      |
| 栈管理     | 每CPU独立栈空间，基于hartid动态计算       | 单个固定栈空间，4KB大小 |
| 同步机制   | 需要内存屏障、启动标志等多核同步原语      | 无需任何同步机制        |
| 内存管理   | 包含分页系统、物理页分配等复杂机制        | 仅BSS清零和栈管理       |
| 设备驱动   | 完整的UART、控制台等驱动                  | 最小化的UART输出功能    |
| 代码复杂度 | 高，数万行代码                            | 极低，约200行代码       |

### 4、设计决策理由

1. **单核设计**：专注于理解启动机制，避免多核复杂性干扰学习目标。
2. **固定栈大小**：4KB足够最小系统使用，且是标准页大小便于管理。
3. **BSS段清零**：遵循C语言规范，确保未初始化变量值为0。
4. **简化UART初始化**：利用QEMU默认配置，仅实现最基本的输出功能。
5. **无限循环结束**：防止程序退出导致QEMU重启，保持系统状态可见。

## 二、实验过程部分

### 1、PPT任务

#### 任务1：理解xv6启动流程

##### kernel/entry.S

---

##### 1. 为什么第一条指令是设置栈指针？

在跳转到C函数（如`start()`）之前，必须设置好栈指针（sp），因为C代码的执行依赖于栈。栈用于：

保存函数返回地址、局部变量、函数参数。

在函数调用时保存寄存器（调用约定）。

---

##### 2. `la sp, stack0` 中的 `stack0` 在哪里定义？

##### `stack0` 在 `start.c` 中定义。通常，它会被定义为全局数组，作为栈的初始位置，例如：

```c
// start.c
__attribute__((aligned(16))) char stack0[4096 * NCPU]; // 每个CPU一个4096字节的栈
```

`la sp, stack0` 是一个伪指令，将 `stack0` 的地址加载到栈指针寄存器（sp）中。

---

##### 3. 为什么要清零BSS段？

​	BSS段存放未初始化的全局变量和静态变量（例如初始值为0的变量）。这些变量在ELF文件中不占用实际空间，但运行时需要分配内存并清零。如果不清零，这些变量可能包含随机垃圾值，导致程序行为不确定（如未初始化变量使用）。通常，在跳转到C主函数之前（或之后立即），需要清零BSS段以确保程序正确初始化。

---

##### 4. 如何从汇编跳转到C函数？

使用 `call` 指令（或 `jal` 指令）跳转到C函数。在代码中：

```assembly
call start   # 跳转到C函数start()
```

具体过程：

1. 设置好栈指针（sp）以便C函数使用栈。
2. 使用 `call start` 调用C函数（`start` 是C函数名）。
3. `call` 指令会将返回地址（下一条指令地址）存入ra寄存器，并跳转到`start`。
4. C函数执行时，可以正确使用栈和寄存器（遵循RISC-V调用约定）。



#####  kernel/kernel.ld 

##### 1. ENTRY(_entry) 的作用是什么？

**作用**：指定程序的入口点。`ENTRY(_entry)` 告诉链接器，程序的执行从符号 `_entry` 开始

##### 2. 为什么代码段要放在0x80000000？

**原因**：为了与QEMU和硬件平台的约定保持一致。

**QEMU约定**：使用 `-kernel` 选项时，QEMU默认将内核映像加载到物理地址 `0x80000000`

**RISC-V架构**：许多RISC-V平台（包括virt机器）使用这个地址作为内核加载地址

**避免冲突**：`0x80000000` 通常位于DRAM的起始区域，避开低地址的设备映射区域

##### 3. etext、edata、end 符号有什么用途？

这些符号用于标识内存中不同段的边界位置：

##### etext（代码段结束）

​	**定义**：`PROVIDE(etext = .);` 在.text段末尾

​	**用途**：标识代码段的结束地址，只读数据段开始的位置

​	**使用示例**：确定代码段的边界，用于内存保护或调试

##### edata（数据段结束）

​	**注意**：在这个脚本中没有显式定义 `edata`，但通常会在.data段末尾定义

**	预期用途**：标识已初始化数据段的结束，BSS段开始的位置

**	实际替代**：可以使用其他方式确定数据段边界

##### end（整个BSS段结束）

​	**定义**：`PROVIDE(end = .);` 在.bss段末尾

​	**用途**：

1. **BSS清零**：在启动代码中清零从edata到end的内存区域
2. **内存管理**：确定内核的结束地址，用于后续内存分配
3. **堆起始**：作为动态内存分配器（堆）的起始位置



#### 深入思考

##### 1. xv6支持多核，你的单核系统可以如何简化？

（1）移除多核栈分配：只需单个4KB栈，删除hartid计算和多栈空间

（2）删除CPU间通信：移除锁、原子操作、核间中断等同步机制  

（3）简化启动流程：所有CPU执行相同代码路径，无需核间协调

（4）移除拓扑发现：不需要检测CPU数量和布局

##### 2. xv6的内存管理很复杂，最小系统需要哪些部分？

**BSS段清零**：初始化未初始化变量为0

**物理内存保护(PMP)**：设置基本访问权限保护

**栈管理**：为内核分配固定栈空间

**简单内存分配**：线性分配的堆管理（可选但推荐）



#### 任务2：设计最小启动流程



![image-20250915161630729](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250915161630729.png)



##### 1. 栈应该放在内存的哪个位置？需要多大？

**位置**：放在BSS段之后的高地址区域（如0x8000B000）
**大小**：4KB（4096字节）足够最小系统使用
**理由**：

​	栈向下增长，与堆的向上增长方向相反，避免冲突

​	4KB是一个内存页大小，便于管理

​	放在高地址是传统做法，避免与代码段冲突

##### 2. 是否需要清零BSS段？为什么？

**需要**，原因：

​	BSS段包含未初始化的全局变量和静态变量

​	这些变量在ELF文件中不占空间，但运行时需要内存

​	如果不清零，变量将包含内存中的随机垃圾值

​	会导致程序行为不确定和难以调试的错误

​	是C语言运行时环境的基本要求

##### 3、最简串口输出需要配置的寄存器

对于最基本的NS16550A兼容串口输出，只需要配置**3个核心寄存器**：

1. 线性控制寄存器 (LCR) - 设置通信格式

```c
#define UART_LCR 3
// 设置：8位数据，无奇偶校验，1位停止位
w_u8(UART_LCR, 0x03);  // 0b00000011
```

2.  FIFO控制寄存器 (FCR) - 启用FIFO

```c
#define UART_FCR 2
// 启用FIFO，清除接收和发送FIFO
w_u8(UART_FCR, 0x07);  // 0b00000111
```

3. 线路状态寄存器 (LSR) - 检查发送就绪

```c
#define UART_LSR 5
// 检查第5位（THRE）是否为1，表示可以发送数据
while ((r_u8(UART_LSR) & 0x20) == 0); // 等待发送就绪
```

最小串口初始化代码

```c
// 串口基地址（QEMU virt机器通常为0x10000000）
#define UART_BASE 0x10000000

// 寄存器读写宏
#define w_u8(reg, val) (*(volatile uint8_t*)(UART_BASE + reg) = val)
#define r_u8(reg) (*(volatile uint8_t*)(UART_BASE + reg))

void uart_init() {
    // 1. 设置通信格式：8N1
    w_u8(3, 0x03); // LCR: 8位数据，无校验，1停止位
    
    // 2. 启用并清空FIFO
    w_u8(2, 0x07); // FCR: 启用FIFO，清空缓冲
}

void uart_putc(char c) {
    // 等待发送缓冲区为空
    while ((r_u8(5) & 0x20) == 0); // 检查LSR第5位（THRE）
    
    // 发送字符
    w_u8(0, c); // 写入数据寄存器
}

void uart_puts(const char *s) {
    while (*s) {
        uart_putc(*s++);
    }
}
```



### 2、实现步骤记录

1. **分析xv6**：阅读xv6的entry.S和kernel.ld，理解启动机制
2. **编写链接脚本**：定义内存布局，设置入口点和各段位置
3. **实现汇编入口**：完成栈设置和BSS清零功能
4. **开发UART驱动**：实现最简字符输出功能
5. **编写主函数**：整合各部分，输出启动信息
6. **测试调试**：通过QEMU运行并验证功能

### 3、问题与解决方案

**问题**：编译时未识别uint64_t等类型

**解决方案**：在main.c中正确定义标准类型别名

### 4、源码理解总结

#### (1)entry.S

1.  **设置栈指针 **
    
    ```assembly
    la sp, stack0      # 将栈底地址 stack0 加载到栈指针寄存器 sp
    li t0, 4096        # 将立即数 4096 (4KB) 加载到临时寄存器 t0
    add sp, sp, t0     # 将 sp 的值加上 4KB，得到栈顶地址
    ```
    **目的**：C 代码的执行（如函数调用、局部变量）依赖于栈。这段代码为 C 环境初始化了栈。

    **细节**：栈通常从高地址向低地址增长。所以代码先将 `sp` 指向栈空间的起始地址（`stack0`），然后加上栈的大小（4KB），使 `sp` 指向栈的**最高地址**（栈顶），为后续的压栈操作做好准备。
    
2.  **清零 BSS 段**
    
    ```assembly
    la a0, _bss_start  # 将 BSS 段的起始地址放入寄存器 a0
    la a1, _bss_end    # 将 BSS 段的结束地址放入寄存器 a1
    j clear_bss_loop_test # 跳转到循环检查条件
    ```
    **目的**：BSS 段用于存放未初始化的全局变量和静态变量。根据 C 语言标准，这些变量在程序开始时应被初始化为 0。操作系统加载器或 QEMU 不会自动完成这个工作，需要启动代码自己处理。
    
    **循环过程**：
    
    `clear_bss_loop_test`: 检查当前地址 `a0` 是否小于结束地址 `a1`。
    
    `clear_bss_loop`: 如果 `a0 < a1`，则执行循环体，将 `zero` 寄存器（恒为 0）的值存储到 `a0` 指向的内存地址，然后将地址指针 `a0` 增加 4 字节（一个字的长度），接着跳回检查条件。
    
    这个循环会一直执行，直到整个 BSS 段（从 `_bss_start` 到 `_bss_end`）都被清零。
    
3.  **跳转到 C 主函数**
    ```assembly
    call main
    ```
    **目的**：在完成了最基本的运行时环境初始化（栈和 BSS 段）后，将控制权交给内核的 C 代码入口点——`main` 函数。

4.  **无限循环 (Spin Loop)**
    ```assembly
    spin:
        j spin
    ```
    **目的**：这是一个安全措施。在操作系统内核中，`main` 函数理论上不应该返回。如果它真的返回了（例如由于错误），这个无限循环可以防止 CPU 执行后续内存中的随机数据（这会导致不可预知的行为，如崩溃），让系统稳定地“挂起”。

#### (2)kernel.ld

1. 基础架构和入口点设置

```ld
OUTPUT_ARCH("riscv")     # 指定目标架构为 RISC-V
ENTRY(_entry)            # 设置程序入口点为 _entry 符号
```

2. 内存起始地址

```ld
. = 0x80000000;          # 设置当前地址计数器为 0x80000000
```
这是 QEMU 加载内核的固定地址

所有后续节区都从这个地址开始排列

3. 节区布局

**文本段 (.text)**

```ld
.text : {
    *(.text .text.*)     # 合并所有目标文件的代码段
    PROVIDE(etext = .);  # 定义 etext 符号标记文本段结束
}
```

**只读数据段 (.rodata)**
```ld
.rodata : {
    *(.rodata .rodata.*)     # 常量数据
    *(.srodata .srodata.*)   # 小型常量数据
}
```

**已初始化数据段 (.data)**
```ld
.data : {
    *(.data .data.*)         # 已初始化的全局变量
    *(.sdata .sdata.*)       # 小型已初始化数据
}
```

**未初始化数据段 (.bss)**
```ld
.bss : {
    . = ALIGN(16);           # 16字节对齐
    _bss_start = .;          # BSS段开始标记（用于启动代码清零）
    *(.bss .bss.*)           # 未初始化的全局变量
    *(.sbss .sbss.*)         # 小型未初始化数据
    . = ALIGN(16);
    _bss_end = .;            # BSS段结束标记
}
```

**栈空间分配**
```ld
.stack : {
    . = ALIGN(16);           # 16字节对齐
    stack0 = .;              # 栈底地址（用于启动代码）
    . += 4096;               # 分配4KB栈空间
}
```



#### (3)uart.c

1. 硬件地址定义

```c
#define UART_BASE 0x10000000UL  // QEMU 虚拟 UART 的基地址
#define THR 0    // 发送保持寄存器（写操作）
#define LSR 5    // 线路状态寄存器（读操作）
#define LSR_TX_IDLE (1 << 5)  // 第5位表示发送器空闲
```

2. 寄存器访问函数

```c
// 读寄存器：将硬件地址转换为易失性指针进行读取
static inline unsigned char uart_read_reg(int reg)
{
    volatile unsigned char *addr = (volatile unsigned char *)(UART_BASE + reg);
    return *addr;
}

// 写寄存器：将值写入对应的硬件地址
static inline void uart_write_reg(int reg, unsigned char val)
{
    volatile unsigned char *addr = (volatile unsigned char *)(UART_BASE + reg);
    *addr = val;
}
```
**`volatile`关键字**：防止编译器优化，确保每次都是真实的硬件访问

**`inline`内联**：提高效率，避免函数调用开销

**字节访问**：使用`unsigned char`确保8位寄存器访问

3. 核心输出功能

```c
void uart_putc(char c)
{
    // 忙等待直到UART就绪（检查LSR_TX_IDLE位）
    while ((uart_read_reg(LSR) & LSR_TX_IDLE) == 0)
        ;
    
    // 发送字符到发送保持寄存器
    uart_write_reg(THR, c);
}
```
**轮询机制**：通过循环检查状态位实现同步操作

**流控制**：确保前一个字符发送完成后再发送下一个

4. 字符串输出封装

```c
void uart_puts(char *s)
{
    while (*s) {
        uart_putc(*s);
        s++;
    }
}
```
基于`uart_putc`实现完整的字符串输出

遍历字符串直到遇到空字符`\0`

5. 初始化函数

```c
void uart_init(void)
{
    // 空实现，依赖QEMU默认配置
}
```


#### (4)main.c

1. 类型定义

```c
typedef unsigned long uint64_t;
typedef unsigned int uint32_t;
typedef unsigned short uint16_t;
typedef unsigned char uint8_t;
```
**目的**：提供标准化的固定宽度整数类型

**意义**：在裸机环境中替代标准库的 `stdint.h`，确保数据类型的一致性

2. 外部符号声明

```c
extern void _bss_start;
extern void _bss_end;
```
**作用**：声明在链接器脚本中定义的符号

**用途**：虽然在此 `main.c` 中未直接使用，但为整个内核提供了 BSS 段的边界信息（可能在启动代码中使用）

3. 函数声明

```c
void uart_init(void);
void uart_puts(char *s);
```
**模块化设计**：声明外部函数接口，与 UART 驱动模块解耦

**前向声明**：确保编译器知道这些函数的存在

4. 主函数实现

```c
void main(void)
{
    // 1. 初始化硬件
    uart_init();
    
    // 2. 输出启动信息
    uart_puts("Hello OS\n");
    uart_puts("Minimal RISC-V OS booted successfully!\n");
    
    // 3. 主循环
    while (1) {
        // 空循环或可添加简单的心跳指示
    }
}
```



## 三、测试验证部分

### 1、功能测试结果

1. **启动测试**：系统正常启动，未出现崩溃或重启现象
2. **输出测试**：成功通过串口输出"Hello OS"和"Minimal RISC-V OS booted successfully!"
3. **BSS清零测试**：验证未初始化变量初始值为0
4. **栈功能测试**：C函数调用和局部变量使用正常

### 2、性能数据

启动时间极短（毫秒级）

### 3、异常测试

1. **栈溢出测试**：故意使用大量栈空间，验证系统行为
2. **UART错误配置**：测试错误基地址时的系统反应
3. **BSS未清零**：注释清零代码，验证变量值不确定性

### 4、运行截图

![image-20250922144332422](C:\Users\lenovo\AppData\Roaming\Typora\typora-user-images\image-20250922144332422.png)







